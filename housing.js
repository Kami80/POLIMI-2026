(() => {
  "use strict";

  const STORAGE_KEY="polimi_home_hunt_v4";
  const PROFILE_KEY="polimi_home_hunt_profile_v2";
  const COMPARE_KEY="polimi_home_hunt_compare_v2";
  const CONTACT_KEY="polimi_home_hunt_contact_v2";
  const WEIGHTS_KEY="polimi_home_hunt_weights_v1";
  const DOCS_KEY="polimi_home_hunt_docs_v1";
  const LEGACY_KEYS=["polimi_home_hunt_v3","polimi_housing_shortlist_v1"];
  const READER_BASE="https://r.jina.ai/";
  const IMPORT_CONFIG=Object.assign({importerApi:"",allowReaderFallback:true,requestTimeoutMs:28000},window.HOME_HUNT_CONFIG||{});
  const $=id=>document.getElementById(id);
  const qs=(s,p=document)=>p.querySelector(s);
  const qsa=(s,p=document)=>[...p.querySelectorAll(s)];

  const CAMPUS={
    leonardo:{name:"Leonardo",full:"Leonardo · Città Studi",address:"Piazza Leonardo da Vinci 32, Milano, Italy"},
    lamasa:{name:"Bovisa La Masa",full:"Bovisa · La Masa / Engineering",address:"Via Giuseppe La Masa 34, Milano, Italy"},
    durando:{name:"Bovisa Durando",full:"Bovisa · Durando / Design",address:"Via Durando 10, Milano, Italy"}
  };
  const TYPE_LABEL={room:"private room",shared:"shared / twin room",studio:"studio",apartment:"apartment",coliving:"private coliving",other:"home"};
  const STATUS={
    saved:{label:"Saved",short:"Saved",color:"#577f9a"},contacted:{label:"Contacted",short:"Contacted",color:"#79658f"},replied:{label:"Replied",short:"Replied",color:"#d08a38"},
    viewing:{label:"Viewing scheduled",short:"Viewing",color:"#4b8a68"},finalist:{label:"Finalist",short:"Finalist",color:"#d96a3c"},chosen:{label:"Chosen home 🎉",short:"Chosen",color:"#3f8b64"},
    rejected:{label:"Rejected",short:"Rejected",color:"#9a6863"},unavailable:{label:"Unavailable",short:"Unavailable",color:"#7b7771"}
  };
  const PIPELINE=["saved","contacted","replied","viewing","finalist","chosen"];
  const AREAS=[
    {id:"citta-studi",name:"Città Studi",aliases:["città studi","citta studi"],commute:{leonardo:8,lamasa:36,durando:34},typical:{room:780,shared:600,studio:1200,apartment:1600,coliving:900},social:82,note:"Closest student-heavy starting point for Leonardo."},
    {id:"lambrate",name:"Lambrate",aliases:["lambrate","udine"],commute:{leonardo:15,lamasa:38,durando:36},typical:{room:700,shared:540,studio:1050,apartment:1450,coliving:820},social:78,note:"Popular east-Milan search area with practical links to Leonardo."},
    {id:"loreto",name:"Loreto / NoLo",aliases:["loreto","nolo","pasteur","rovereto"],commute:{leonardo:20,lamasa:31,durando:29},typical:{room:740,shared:560,studio:1150,apartment:1550,coliving:850},social:88,note:"Useful middle ground for transit, student life, and city access."},
    {id:"porta-venezia",name:"Porta Venezia / Buenos Aires",aliases:["porta venezia","corso buenos aires","buenos aires","venezia"],commute:{leonardo:18,lamasa:32,durando:30},typical:{room:820,shared:630,studio:1250,apartment:1700,coliving:930},social:92,note:"Lively east-central option with a practical route toward Leonardo."},
    {id:"centrale",name:"Centrale",aliases:["centrale","stazione centrale","repubblica"],commute:{leonardo:22,lamasa:28,durando:26},typical:{room:830,shared:640,studio:1300,apartment:1750,coliving:950},social:75,note:"Highly connected when campus and city access both matter."},
    {id:"isola",name:"Isola / Garibaldi",aliases:["isola","garibaldi","porta nuova"],commute:{leonardo:30,lamasa:22,durando:20},typical:{room:900,shared:700,studio:1450,apartment:1950,coliving:1050},social:94,note:"Strong social location with good access toward Bovisa."},
    {id:"bovisa",name:"Bovisa",aliases:["bovisa","dergano"],commute:{leonardo:38,lamasa:9,durando:8},typical:{room:650,shared:500,studio:980,apartment:1350,coliving:760},social:73,note:"The obvious first search area for Bovisa campuses."},
    {id:"affori",name:"Affori",aliases:["affori","comasina","bruzzano"],commute:{leonardo:43,lamasa:21,durando:20},typical:{room:620,shared:480,studio:950,apartment:1300,coliving:730},social:58,note:"Worth checking when budget matters more than centrality."},
    {id:"certosa",name:"Certosa / Villapizzone",aliases:["certosa","villapizzone","musocco"],commute:{leonardo:45,lamasa:18,durando:17},typical:{room:640,shared:500,studio:1000,apartment:1380,coliving:750},social:57,note:"Practical for Bovisa and western connections."},
    {id:"navigli",name:"Navigli",aliases:["navigli","porta genova","romolo"],commute:{leonardo:38,lamasa:38,durando:36},typical:{room:880,shared:690,studio:1400,apartment:1900,coliving:1030},social:100,note:"Best when nightlife matters more than campus proximity."},
    {id:"porta-romana",name:"Porta Romana",aliases:["porta romana","lodi","corvetto"],commute:{leonardo:28,lamasa:42,durando:40},typical:{room:820,shared:640,studio:1300,apartment:1750,coliving:940},social:83,note:"Balanced south-east option with strong city access."},
    {id:"bicocca",name:"Bicocca / Precotto",aliases:["bicocca","precotto","greco"],commute:{leonardo:32,lamasa:33,durando:31},typical:{room:650,shared:510,studio:1000,apartment:1380,coliving:760},social:69,note:"Broader student area to test when value matters."},
    {id:"unknown",name:"Area not set",aliases:[],commute:{leonardo:null,lamasa:null,durando:null},typical:{room:750,shared:580,studio:1150,apartment:1550,coliving:850},social:50,note:"Set the area manually."}
  ];
  const PLATFORMS=[
    {id:"housinganywhere",name:"HousingAnywhere",badge:"Direct import",accent:"#6f54d9",initial:"HA",text:"A strong option for international students, furnished rooms, and mid-term stays.",tips:["International workflow","Furnished options","Availability often shown"],urls:{room:"https://housinganywhere.com/s/Milan--Italy/student-room",shared:"https://housinganywhere.com/s/Milan--Italy/student-room",coliving:"https://housinganywhere.com/s/Milan--Italy/private-rooms",studio:"https://housinganywhere.com/s/Milan--Italy/apartment-for-rent",apartment:"https://housinganywhere.com/s/Milan--Italy/apartment-for-rent"}},
    {id:"spotahome",name:"Spotahome",badge:"Direct import",accent:"#ef5a59",initial:"S",text:"Useful when you are still outside Milan and want furnished rooms or apartments with a remote-first flow.",tips:["Remote-first flow","Rooms + apartments","Furnished focus"],urls:{room:"https://www.spotahome.com/s/milan/for-rent%3Arooms",shared:"https://www.spotahome.com/s/milan/for-rent%3Arooms",coliving:"https://www.spotahome.com/s/milan/for-rent%3Arooms",studio:"https://www.spotahome.com/s/milan/for-rent%3Aapartments",apartment:"https://www.spotahome.com/s/milan/for-rent%3Aapartments"}}
  ];
  const ACTIVE_IMPORTERS=new Set(["housinganywhere","spotahome"]);
  const DOCS=[
    ["identity","Identity document ready","Track readiness only; do not upload it here."],["enrollment","Admission / enrollment proof","Useful when a landlord asks for student status."],["income","Income / scholarship evidence","Prepare only if requested by the landlord/agency."],["guarantor","Guarantor information","If your rental situation requires one."],["references","Previous landlord/reference details","Optional, but useful when requested."],["tax","Codice fiscale plan","Know how/when you will obtain or provide it if required."]
  ];
  const DEFAULT_WEIGHTS={budget:40,commute:30,upfront:15,furnished:10,completeness:5};
  const WEIGHT_LABELS={budget:"Monthly affordability",commute:"Campus commute",upfront:"Upfront cash",furnished:"Furnished fit",completeness:"Listing completeness"};

  let homes=[]; let compareIds=[]; let favoriteOnly=false; let messageLanguage="english"; let currentImported=null; let currentConfidence={};
  let duplicateState=null; let diffState=null; let currentWorkspaceId=null; let workspaceTab="overview"; let deferredInstallPrompt=null; let toastTimer=null;

  function escapeHtml(v){return String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));}
  const escapeAttr=escapeHtml;
  function normalizeText(v){return String(v||"").replace(/\s+/g," ").trim();}
  function safeNumber(v){const n=Number(v);return Number.isFinite(n)&&n>=0?n:null;}
  function hasValue(v){return v!==null&&v!==undefined&&v!==""&&!(typeof v==="number"&&Number.isNaN(v));}
  function uid(){return globalThis.crypto?.randomUUID?.()||`home-${Date.now()}-${Math.random().toString(36).slice(2,9)}`;}
  function euro(v){const n=Number(v);return Number.isFinite(n)?new Intl.NumberFormat("en-IE",{style:"currency",currency:"EUR",maximumFractionDigits:0}).format(n):"—";}
  function fmtDate(v){if(!v)return"—";const d=new Date(v);return Number.isNaN(d.getTime())?String(v):new Intl.DateTimeFormat("en",{day:"numeric",month:"short",year:"numeric"}).format(d);}
  function fmtDateTime(v){if(!v)return"—";const d=new Date(v);return Number.isNaN(d.getTime())?String(v):new Intl.DateTimeFormat("en",{day:"numeric",month:"short",hour:"2-digit",minute:"2-digit"}).format(d);}
  const todayIso=()=>new Date().toISOString().slice(0,10);
  function areaById(id){return AREAS.find(a=>a.id===id)||AREAS.at(-1);}
  function statusLabel(s){return STATUS[s]?.label||STATUS.saved.label;}
  function displayLocation(h){const a=normalizeText(h?.address||""),bad=/\b(is vibrant|will find|property|supermarket|points of interest|nearby|close to the)\b/i.test(a)||/[.!?]/.test(a);if(a&&a.length<=86&&!bad)return a;const area=areaById(h?.areaId).name;return area==="Area not set"?"Location needs review":area;}
  function shortHomeTitle(h){const address=displayLocation(h);if(address!=="Location needs review"&&address.length<=62&&/\b(via|viale|corso|piazza|largo|alzaia|buenos aires|porta venezia)\b/i.test(address)){return address.replace(/,\s*(milano|milan)(?:,.*)?$/i,"").trim();}let t=normalizeText(h?.title||"");t=t.replace(/\(\s*ref\s*:\s*[^)]+\)/ig,"").replace(/^private\s+room\s+for\s+rent\s+in\s+milan,?\s*/i,"").replace(/^room\s+in\s+shared\s+apartment\s+in\s*/i,"").replace(/,\s*(milano|milan)\s*$/i,"").trim();if(!t)t=areaById(h?.areaId).name||"Saved home";return t.length>42?`${t.slice(0,39).trim()}…`:t;}
  function toast(msg){const n=$("housingToast");if(!n)return;n.textContent=msg;n.classList.add("show");clearTimeout(toastTimer);toastTimer=setTimeout(()=>n.classList.remove("show"),2600);}
  async function copyText(text,msg="Copied"){if(!text)return;try{await navigator.clipboard.writeText(text);}catch(_){const t=document.createElement("textarea");t.value=text;t.style.cssText="position:fixed;opacity:0";document.body.append(t);t.select();document.execCommand("copy");t.remove();}toast(msg);}
  function downloadText(filename,text,type="text/plain"){const blob=new Blob([text],{type});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=filename;a.click();setTimeout(()=>URL.revokeObjectURL(url),500);}
  function daysBetween(a,b){const x=new Date(a);const y=new Date(b);return Math.round((y-x)/86400000);}

  function initTheme(){const apply=t=>{document.documentElement.dataset.theme=t;$("housingThemeIcon").textContent=t==="dark"?"light_mode":"dark_mode";const meta=document.querySelector('meta[name="theme-color"]');if(meta)meta.content=t==="dark"?"#211d1a":"#B84A32";localStorage.setItem("polimi-theme",t);};const stored=localStorage.getItem("polimi-theme")||localStorage.getItem("polimi_theme");apply(stored||(matchMedia("(prefers-color-scheme: dark)").matches?"dark":"light"));$("housingThemeToggle")?.addEventListener("click",()=>apply(document.documentElement.dataset.theme==="dark"?"light":"dark"));}

  function defaultProfile(){const d=new Date();if(d.getMonth()>8)d.setFullYear(d.getFullYear()+1);d.setMonth(8,1);return{campus:"leonardo",type:"room",budget:800,commute:30,moveIn:d.toISOString().slice(0,10),priority:"balanced"};}
  function loadProfile(){try{return{...defaultProfile(),...JSON.parse(localStorage.getItem(PROFILE_KEY)||"{}")};}catch(_){return defaultProfile();}}
  function getProfile(){return{campus:$("profileCampus").value,type:$("profileType").value,budget:Math.max(0,Number($("profileBudget").value)||0),commute:Math.max(0,Number($("profileCommute").value)||0),moveIn:$("profileMoveIn").value,priority:$("profilePriority").value};}
  function saveProfile(){localStorage.setItem(PROFILE_KEY,JSON.stringify(getProfile()));renderSearchGuide();renderAll();}
  function initProfile(){const p=loadProfile();$("profileCampus").value=p.campus;$("profileType").value=p.type;$("profileBudget").value=p.budget;$("profileCommute").value=p.commute;$("profileMoveIn").value=p.moveIn||"";$("profilePriority").value=p.priority;["profileCampus","profileType","profileBudget","profileCommute","profileMoveIn","profilePriority"].forEach(id=>$(id).addEventListener(id==="profileBudget"?"input":"change",saveProfile));renderSearchGuide();}
  function scoreGuideArea(area,p){if(area.id==="unknown")return-1;const c=area.commute[p.campus];const target=area.typical[p.type]||area.typical.room;const bs=p.budget>=target?100:Math.max(10,100-((target-p.budget)/target)*110);const cs=c<=p.commute?100-Math.max(0,c-10)*1.2:Math.max(5,60-(c-p.commute)*4);if(p.priority==="budget")return bs*.6+cs*.3+area.social*.1;if(p.priority==="commute")return cs*.65+bs*.25+area.social*.1;if(p.priority==="social")return area.social*.45+cs*.3+bs*.25;return bs*.4+cs*.45+area.social*.15;}
  function rankedGuideAreas(){const p=getProfile();return AREAS.filter(a=>a.id!=="unknown").map(a=>({...a,guideScore:Math.round(scoreGuideArea(a,p))})).sort((a,b)=>b.guideScore-a.guideScore);}
  function searchBriefText(){const p=getProfile(),areas=rankedGuideAreas().slice(0,4);return`Milan Home Hunt search brief\nCampus: Polimi ${CAMPUS[p.campus].full}\nHousing: ${TYPE_LABEL[p.type]}\nMaximum monthly housing budget: ${euro(p.budget)}\nMaximum commute: ${p.commute} minutes\nMove-in: ${p.moveIn?fmtDate(`${p.moveIn}T12:00:00`):"flexible"}\nPriority: ${p.priority}\nStart with: ${areas.map(a=>a.name).join(", ")}\n\nBefore saving: verify rent, recurring charges, deposit, fees, contract, availability, exact address, and who you are paying.`;}
  function renderSearchGuide(){if(!$("searchBriefBox"))return;const p=getProfile(),areas=rankedGuideAreas().slice(0,4),typeLabel=TYPE_LABEL[p.type],typeSearchLabel=typeLabel.endsWith("room")?`${typeLabel}s`:typeLabel;$("searchBriefBox").innerHTML=`<strong>YOUR SEARCH BRIEF</strong>${escapeHtml(typeLabel)} · up to <b>${euro(p.budget)}</b> · ${escapeHtml(CAMPUS[p.campus].name)} · ≤ ${p.commute} min · ${escapeHtml(p.moveIn?fmtDate(`${p.moveIn}T12:00:00`):"flexible")}`;$("areaRecommendations").innerHTML=areas.map((a,i)=>`<div class="area-rec"><i>${String(i+1).padStart(2,"0")}</i><div><strong>${escapeHtml(a.name)}</strong><small>${escapeHtml(a.note)}</small></div><span>~${a.commute[p.campus]} min<br>${euro(a.typical[p.type])} typical</span></div>`).join("");$("platformGrid").innerHTML=PLATFORMS.map(x=>`<article class="platform-card"><div class="platform-top"><div class="platform-logo" style="background:${x.accent}">${x.initial}</div><div class="platform-copy"><h3>${x.name}</h3><small>${x.badge}</small></div></div><p>${x.text}</p><div class="platform-meta"><span class="platform-focus">For ${escapeHtml(typeSearchLabel)}</span></div><div class="platform-tips">${x.tips.map(t=>`<span>${t}</span>`).join("")}</div><a class="hunt-secondary-btn platform-open-btn" href="${escapeAttr(x.urls[p.type]||x.urls.room)}" target="_blank" rel="noopener noreferrer"><span>Open search</span><span class="material-symbols-rounded">open_in_new</span></a></article>`).join("");const ha=PLATFORMS.find(x=>x.id==="housinganywhere"),sp=PLATFORMS.find(x=>x.id==="spotahome");if($("pinnedHousingAnywhere")&&ha)$("pinnedHousingAnywhere").href=ha.urls[p.type]||ha.urls.room;if($("pinnedSpotahome")&&sp)$("pinnedSpotahome").href=sp.urls[p.type]||sp.urls.room;qsa("[data-campus-card]").forEach(card=>card.classList.toggle("active",card.dataset.campus===p.campus));}


  function sourceFromUrl(url){try{const host=new URL(url).hostname.toLowerCase().replace(/^www\./,"");if(host.endsWith("idealista.it"))return{id:"idealista",label:"Idealista",host};if(host.endsWith("immobiliare.it"))return{id:"immobiliare",label:"Immobiliare.it",host};if(host.endsWith("housinganywhere.com"))return{id:"housinganywhere",label:"HousingAnywhere",host};if(host.endsWith("spotahome.com"))return{id:"spotahome",label:"Spotahome",host};return{id:"other",label:host,host};}catch(_){return{id:"other",label:"Unknown source",host:""};}}
  function normalizeUrl(raw){const v=String(raw||"").trim();if(!v)return"";const x=/^https?:\/\//i.test(v)?v:`https://${v}`;try{const u=new URL(x);if(!/^https?:$/.test(u.protocol))return"";u.hash="";["utm_source","utm_medium","utm_campaign","utm_term","utm_content"].forEach(k=>u.searchParams.delete(k));return u.toString();}catch(_){return"";}}
  function updateSourceDetection(){const url=normalizeUrl($("listingUrl").value);const el=$("sourceDetected");if(!url){el.className="source-detected";el.innerHTML="<i></i><b>Waiting for a link</b>";return;}const s=sourceFromUrl(url),active=ACTIVE_IMPORTERS.has(s.id);el.className=`source-detected ${active?"detected":"paused"}`;el.innerHTML=`<i></i><b>${escapeHtml(s.label)}${active?" detected":" · text/manual"}</b>`;}
  function progress(show,p=12,text="Reading listing…"){const w=$("importProgress");w.hidden=!show;if(show){$("importProgressBar").style.width=`${p}%`;$("importProgressText").textContent=text;}}
  function showImportAlert(msg,type="error"){const a=$("importAlert");a.hidden=false;a.className=`import-alert ${type}`;a.innerHTML=msg;}
  function clearImportAlert(){$("importAlert").hidden=true;}
  const IMPORT_STEP_ORDER=["detect","fetch","extract","analyze","review"];
  function setImportStep(key,state="active"){const box=$("importStepList");if(!box)return;box.hidden=false;const idx=IMPORT_STEP_ORDER.indexOf(key);qsa("[data-import-step]",box).forEach((el,i)=>{el.classList.remove("active","done");if(i<idx||state==="done"&&i===idx)el.classList.add("done");else if(i===idx)el.classList.add("active");});}
  function hideImportSteps(){const box=$("importStepList");if(box){box.hidden=true;qsa("[data-import-step]",box).forEach(el=>el.classList.remove("active","done"));}}
  async function fetchListingText(url,signal){const r=await fetch(`${READER_BASE}${url}`,{headers:{Accept:"text/plain"},cache:"no-store",signal});if(!r.ok)throw new Error(`Reader returned ${r.status}`);const t=await r.text();if(!t||t.length<160)throw new Error("Too little readable content");return t;}
  function apiPropertyToHome(data,url){const p=data?.property||{},src=sourceFromUrl(url),context=[p.address,p.title,p.description].filter(Boolean).join(" ");const h=blankHome(url);Object.assign(h,{canonicalUrl:p.canonicalUrl||data.canonicalUrl||url,source:data.provider||src.label,sourceId:data.providerId||src.id,title:normalizeText(p.title)||"Rental listing",rent:p.rent===null||p.rent===undefined?null:safeNumber(p.rent),bills:p.bills===null||p.bills===undefined?null:safeNumber(p.bills),billsStatus:p.billsStatus||"unknown",deposit:p.deposit===null||p.deposit===undefined?null:safeNumber(p.deposit),agencyFee:p.agencyFee===null||p.agencyFee===undefined?null:safeNumber(p.agencyFee),type:p.type&&TYPE_LABEL[p.type]?p.type:detectType(context),areaId:detectArea(context),address:normalizeText(p.address),size:p.size===null||p.size===undefined?null:safeNumber(p.size),rooms:p.rooms===null||p.rooms===undefined?null:safeNumber(p.rooms),bedrooms:p.bedrooms===null||p.bedrooms===undefined?null:safeNumber(p.bedrooms),bathrooms:p.bathrooms===null||p.bathrooms===undefined?null:safeNumber(p.bathrooms),floor:normalizeText(p.floor),elevator:p.elevator||"unknown",balcony:p.balcony||"unknown",airConditioning:p.airConditioning||"unknown",minimumStay:normalizeText(p.minimumStay),landlordName:normalizeText(p.landlordName),available:normalizeText(p.available),furnished:p.furnished||"unknown",contract:p.contract||"unknown",description:normalizeText(p.description),image:p.image||p.images?.[0]||"",images:Array.isArray(p.images)?p.images.slice(0,12):[],importMethod:data.fetchMode==="reader-fallback"?"api-reader-fallback":"api",lastChecked:Date.now(),sourceSnapshotAt:Date.now()});h.confidence={...buildConfidence(h,true),...(data.confidence||{})};if(h.areaId!=="unknown"&&!h.confidence.areaId)h.confidence.areaId="medium";return h;}
  async function fetchImporterApi(url,signal){const endpoint=String(IMPORT_CONFIG.importerApi||"").trim();if(!endpoint){const e=new Error("Importer API is not configured");e.code="NO_API";throw e;}const r=await fetch(endpoint,{method:"POST",headers:{"content-type":"application/json","accept":"application/json"},body:JSON.stringify({url}),cache:"no-store",signal});let data={};try{data=await r.json();}catch(_){}if(!r.ok||!data.success)throw new Error(data.error||`Importer returned ${r.status}`);return apiPropertyToHome(data,url);}
  async function retrieveListing(url,signal){let apiError=null;if(IMPORT_CONFIG.importerApi){try{return await fetchImporterApi(url,signal);}catch(err){apiError=err;if(!IMPORT_CONFIG.allowReaderFallback)throw err;}}if(!IMPORT_CONFIG.allowReaderFallback)throw apiError||new Error("Importer API is not configured");const text=await fetchListingText(url,signal),parsed=parseListing(text,url);parsed.importMethod="reader";return parsed;}
  async function checkImporterStatus(){const pill=$("importerStatus");if(!pill)return;const endpoint=String(IMPORT_CONFIG.importerApi||"").trim();if(!endpoint){pill.classList.add("fallback");pill.innerHTML=`<span class="material-symbols-rounded">cloud_off</span><span>Reader fallback · connect Worker for secure import</span>`;return;}try{const u=new URL(endpoint),health=`${u.origin}/health`,r=await fetch(health,{cache:"no-store"});if(!r.ok)throw new Error();pill.classList.remove("fallback");pill.classList.add("ready");pill.innerHTML=`<span class="material-symbols-rounded">shield_lock</span><span>Secure importer connected</span>`;}catch(_){pill.classList.add("fallback");pill.innerHTML=`<span class="material-symbols-rounded">warning</span><span>Importer offline · reader fallback available</span>`;}}
  function normalizeNumericString(raw){let t=String(raw||"").trim().replace(/\s/g,"");if(!t)return null;if(/^\d{1,3}\.\d{3}(?:\.\d{3})*$/.test(t))t=t.replace(/\./g,"");else if(/^\d{1,3},\d{3}(?:,\d{3})*$/.test(t))t=t.replace(/,/g,"");else t=t.replace(/,(\d{1,2})$/,'.$1').replace(/[^\d.]/g,"");const n=Number(t);return Number.isFinite(n)?n:null;}
  function moneyValuesFromLine(line){const f=[];[/(?:€\s*|EUR\s*)(\d[\d.,\s]{0,8})/gi,/(\d[\d.,\s]{0,8})\s*€/gi].forEach(p=>{let m;while((m=p.exec(line))){const n=normalizeNumericString(m[1]);if(n!==null&&n<=100000)f.push({value:n,index:m.index});}});f.sort((a,b)=>a.index-b.index);return [...new Map(f.map(x=>[x.value,x])).values()].map(x=>x.value);}
  const relevantLines=t=>t.split(/\r?\n/).map(normalizeText).filter(Boolean);
  function extractPrice(lines){const a=[];lines.slice(0,220).forEach((line,index)=>{const lower=line.toLowerCase(),monthly=/(\/\s*(?:mese|month)|al\s+mese|per\s+month|mensil|month\b)/i.test(line);moneyValuesFromLine(line).forEach(value=>{if(value<150||value>8000)return;let score=monthly?5:0;if(/rent|affitto|canone|camera|stanza|room|studio|apartment|appartamento/i.test(lower))score+=3;if(/deposit|cauzione|fee|commission|spese|utilities|utenze|bills/i.test(lower))score-=4;score+=Math.max(0,2-index/100);a.push({value,score,index});});});a.sort((a,b)=>b.score-a.score||a.index-b.index);return a[0]?.value??null;}
  function extractTaggedMoney(text,terms,exclusions=[]){const lower=text.toLowerCase();for(const term of terms){let from=0;while(from<lower.length){const index=lower.indexOf(term.toLowerCase(),from);if(index<0)break;const start=Math.max(0,index-45),ctx=lower.slice(start,Math.min(text.length,index+term.length+45));if(!exclusions.some(e=>ctx.includes(e.toLowerCase()))){const after=moneyValuesFromLine(text.slice(index+term.length,Math.min(text.length,index+term.length+115))).filter(v=>v<=10000);if(after.length)return after[0];const around=moneyValuesFromLine(text.slice(start,Math.min(text.length,index+term.length+135))).filter(v=>v<=10000);if(around.length)return around[0];}from=index+term.length;}}return null;}
  function extractTitle(text,lines,source){const meta=text.match(/^Title:\s*(.+)$/mi)?.[1],heads=lines.filter(l=>/^#{1,2}\s+/.test(l)).map(l=>l.replace(/^#{1,2}\s+/,"").trim()),c=[meta,...heads].filter(Boolean).filter(x=>x.length>5&&x.length<180),generic=[source.label.toLowerCase(),"idealista","immobiliare","housinganywhere","spotahome"];return normalizeText(c.find(x=>!generic.some(g=>x.toLowerCase()===g))||c[0]||"Rental listing");}
  function extractImage(text){const m=[...text.matchAll(/!\[[^\]]*\]\((https?:\/\/[^)\s]+)\)/g)].map(x=>x[1]);return m.find(u=>!/logo|icon|avatar|sprite|tracking|pixel/i.test(u))||"";}
  function extractImages(text){return [...new Set([...text.matchAll(/!\[[^\]]*\]\((https?:\/\/[^)\s]+)\)/g)].map(x=>x[1]).filter(u=>!/logo|icon|avatar|sprite|tracking|pixel/i.test(u)))].slice(0,8);}
  function detectArea(text){const lower=text.toLowerCase();let best="unknown",pos=Infinity;AREAS.filter(a=>a.id!=="unknown").forEach(a=>a.aliases.forEach(alias=>{const i=lower.indexOf(alias.toLowerCase());if(i>=0&&i<pos){best=a.id;pos=i;}}));return best;}
  function detectType(text){const l=text.toLowerCase();if(/shared room|twin room|posto letto|camera doppia/.test(l))return"shared";if(/private room|single room|camera singola|stanza singola|camera in|stanza in/.test(l))return"room";if(/monolocale|studio apartment|\bstudio\b/.test(l))return"studio";if(/coliving|co-living/.test(l))return"coliving";if(/apartment|appartamento|bilocale|trilocale|flat/.test(l))return"apartment";return getProfile().type||"other";}
  function extractAddress(lines){const a=lines.find(l=>/\b(via|viale|corso|piazza|largo|vicolo|alzaia)\b/i.test(l)&&/milano|milan/i.test(l)&&l.length<190)||lines.find(l=>/\b(via|viale|corso|piazza|largo|alzaia)\s+[A-ZÀ-Ý]/i.test(l)&&l.length<150);return a?a.replace(/^#{1,4}\s+/,"").replace(/^[-*]\s+/,""):"";}
  function extractAvailability(lines){const t=lines.slice(0,500).join(" ");if(/available\s+now/i.test(t))return"Available now";if(/disponibil[ei]\s+subito|liber[ao]\s+subito/i.test(t))return"Disponibile subito";for(const p of[/available\s+from\s+([^|•.;]{2,45})/i,/disponibil[ei]\s+da\s+([^|•.;]{2,45})/i,/liber[ao]\s+da\s+([^|•.;]{2,45})/i]){const m=t.match(p);if(m)return normalizeText(m[0]).slice(0,90);}return"";}
  function extractDescription(lines){const ignored=/cookie|privacy|login|sign in|contatta|chiama|telefono|salva|share|menu|filter|sort|image:/i;return normalizeText(lines.filter(l=>l.length>=120&&l.length<=900&&!ignored.test(l)&&!/^https?:\/\//.test(l)).sort((a,b)=>b.length-a.length)[0]||"").slice(0,900);}
  function parseListing(text,url){const source=sourceFromUrl(url),lines=relevantLines(text),joined=lines.slice(0,650).join("\n"),lower=joined.toLowerCase(),rent=extractPrice(lines),noDeposit=/no\s+deposit|senza\s+cauzione|non\s+si\s+richiede\s+cauzione/.test(lower),noAgency=/no\s+agency\s+fee|senza\s+commissioni|nessuna\s+commissione|no\s+commissione|senza\s+agenzia/.test(lower),deposit=noDeposit?0:extractTaggedMoney(joined,["security deposit","deposito cauzionale","deposit","cauzione"],["no deposit","senza cauzione"]),agencyFee=noAgency?0:extractTaggedMoney(joined,["agency fee","commissione di agenzia","provvigione","commission","agenzia"],["senza commission","nessuna commission","no agency"]),partial=/some utilities included|alcune utenze incluse/.test(lower),included=!partial&&/bills\s+included|utilities\s+included|utenze\s+incluse|spese\s+incluse|tutte\s+le\s+spese\s+incluse/.test(lower),extra=/bills\s+excluded|utenze\s+(?:escluse|a parte)|spese\s+(?:escluse|a parte)/.test(lower);let bills=extractTaggedMoney(joined,["spese condominiali","spese mensili","monthly charges","utilities","utenze","bills","charges","spese"],["spese di agenzia","agency fee"]);if(included)bills=0;if(bills!==null&&rent!==null&&bills===rent)bills=null;const size=joined.match(/\b(\d{1,3})\s*(?:m²|m2|mq|m\^2)/i),rooms=joined.match(/\b(\d{1,2})\s+(?:bedrooms?|rooms?|locali|camere)\b/i),furnished=/unfurnished|non\s+arredat/.test(lower)?"no":/furnished|arredat|ammobiliat/.test(lower)?"yes":"unknown",contract=/contratto|rental contract|lease agreement|tenancy agreement/.test(lower)?"yes":"unknown",images=extractImages(text);const result=blankHome(url);Object.assign(result,{source:source.label,sourceId:source.id,title:extractTitle(text,lines,source),rent,bills,billsStatus:partial?"partial":included?"included":extra?"extra":bills!==null?"extra":"unknown",deposit,agencyFee,type:detectType(joined),areaId:detectArea(joined),address:extractAddress(lines),size:size?Number(size[1]):null,rooms:rooms?Number(rooms[1]):null,available:extractAvailability(lines),furnished,contract,description:extractDescription(lines),image:images[0]||extractImage(text),images,importMethod:"reader",lastChecked:Date.now(),sourceSnapshotAt:Date.now()});result.confidence=buildConfidence(result,true);return result;}

  function blankHome(url=""){const s=sourceFromUrl(url);return{id:uid(),url,canonicalUrl:url,source:s.label,sourceId:s.id,title:"Rental listing",rent:null,bills:null,billsStatus:"unknown",deposit:null,agencyFee:null,type:getProfile()?.type||"room",areaId:"unknown",address:"",size:null,rooms:null,bedrooms:null,bathrooms:null,floor:"",elevator:"unknown",balcony:"unknown",airConditioning:"unknown",minimumStay:"",landlordName:"",available:"",furnished:"unknown",contract:"unknown",description:"",image:"",images:[],notes:"",status:"saved",favorite:false,createdAt:Date.now(),updatedAt:Date.now(),lastChecked:null,importMethod:"manual",confidence:{},verifiedCommute:{transit:null,walk:null,bike:null,checkedAt:null},history:[],nextAction:{type:"",date:"",note:""},health:{status:"new",changedAt:null,message:"Not checked yet"},sourceSnapshot:null};}
  function buildConfidence(h,imported=false){const c={};["title","source","rent","bills","billsStatus","deposit","agencyFee","type","areaId","address","size","rooms","bedrooms","bathrooms","floor","elevator","balcony","airConditioning","minimumStay","landlordName","available","furnished","contract","description"].forEach(k=>{const v=h[k];const known=hasValue(v)&&v!=="unknown"&&v!=="Rental listing"&&v!=="Area not set";c[k]=known?(imported?"high":"medium"):"low";});if(h.areaId!=="unknown")c.area="medium";return c;}
  function normalizeHome(h){const b=blankHome(h?.url||"");const out={...b,...h,id:h?.id||b.id,verifiedCommute:{...b.verifiedCommute,...(h?.verifiedCommute||{})},nextAction:{...b.nextAction,...(h?.nextAction||{})},health:{...b.health,...(h?.health||{})}};out.history=Array.isArray(h?.history)?h.history:[];out.images=Array.isArray(h?.images)?h.images:out.image?[out.image]:[];if(!out.areaId||out.areaId==="unknown")out.areaId=detectArea([out.address,out.title,out.description].filter(Boolean).join(" "));out.confidence=h?.confidence||buildConfidence(out,["reader","api","api-reader-fallback","pasted-text"].includes(out.importMethod));return out;}
  function importCompleteness(h){const checks=[h.rent!==null,h.billsStatus!=="unknown",h.deposit!==null,h.agencyFee!==null,h.areaId!=="unknown",!!h.available,h.contract!=="unknown",!!h.address,h.size!==null,h.furnished!=="unknown"];return Math.round(checks.filter(Boolean).length/checks.length*100);}
  function estimatedBills(h){if(h.billsStatus==="included")return 0;if(h.bills!==null)return Number(h.bills)||0;return h.type==="apartment"||h.type==="studio"?160:110;}
  function monthlyEstimate(h){return h.rent===null?null:Number(h.rent)+estimatedBills(h);}
  function upfrontEstimate(h){if(h.rent===null)return null;return Number(h.rent)+(h.deposit??0)+(h.agencyFee??0)+(h.billsStatus==="included"?0:(h.bills??0));}
  function firstYearEstimate(h){const m=monthlyEstimate(h);return m===null?null:m*12+(h.agencyFee??0);}
  function commuteEstimate(h){return h.verifiedCommute?.transit??areaById(h.areaId).commute[getProfile().campus]??null;}
  function commuteLabel(h){const v=commuteEstimate(h);return v===null?"—":`${v} min${h.verifiedCommute?.transit?" verified":" est."}`;}
  function getWeights(){try{return{...DEFAULT_WEIGHTS,...JSON.parse(localStorage.getItem(WEIGHTS_KEY)||"{}")};}catch(_){return{...DEFAULT_WEIGHTS};}}
  function componentScores(h){const p=getProfile(),m=monthlyEstimate(h),c=commuteEstimate(h),u=upfrontEstimate(h);return{
    budget:m===null?35:m<=p.budget?100:Math.max(0,100-((m-p.budget)/Math.max(p.budget,1))*135),
    commute:c===null?35:c<=p.commute?100-Math.max(0,c-10)*.8:Math.max(0,65-(c-p.commute)*4),
    upfront:u===null?40:u<=p.budget*2.5?100:u<=p.budget*4?70:Math.max(10,70-((u-p.budget*4)/Math.max(p.budget,1))*18),
    furnished:h.furnished==="yes"?100:h.furnished==="partial"?70:h.furnished==="unknown"?50:25,
    completeness:importCompleteness(h)
  };}
  function fitScore(h){const w=getWeights(),s=componentScores(h),sum=Object.values(w).reduce((a,b)=>a+Number(b||0),0)||1;return Math.round(Object.keys(w).reduce((acc,k)=>acc+s[k]*Number(w[k]||0),0)/sum);}
  function valueScore(h){const m=monthlyEstimate(h);if(m===null)return 0;return Math.round((fitScore(h)*1000)/Math.max(m,1));}
  function warningList(h){const w=[];if(h.rent===null)w.push("Monthly rent was not captured.");if(h.billsStatus==="unknown")w.push("Monthly bills / condominium charges are unclear.");if(h.deposit===null)w.push("Deposit amount is missing.");if(h.agencyFee===null)w.push("Agency/admin fees are unclear.");if(h.contract==="unknown")w.push("Contract details were not detected.");if(h.areaId==="unknown"&&!h.address)w.push("Location is too vague for a useful commute check.");if(!h.available)w.push("Availability / move-in date needs confirmation.");if(h.furnished==="unknown")w.push("Furnishing status is unclear.");if(h.sourceId==="other")w.push("Verify this rental source independently.");if(h.rent!==null&&h.rent>0&&h.rent<300)w.push("Advertised monthly amount is unusually small; verify what it represents.");return w;}
  function verificationItems(h){return[
    ["Rent",h.rent!==null,h.rent!==null?euro(h.rent):"Missing"],["Bills",h.billsStatus!=="unknown",h.billsStatus==="included"?"Included":h.bills!==null?euro(h.bills):"Needs clarification"],["Deposit",h.deposit!==null,h.deposit!==null?euro(h.deposit):"Missing"],["Fees",h.agencyFee!==null,h.agencyFee===0?"None stated":h.agencyFee!==null?euro(h.agencyFee):"Missing"],["Contract",h.contract!=="unknown",h.contract==="yes"?"Mentioned":"Not clear"],["Availability",!!h.available,h.available||"Missing"],["Exact location",!!h.address,h.address||"Missing"],["Source health",h.health?.status!=="unreachable",h.health?.status==="changed"?"Changes waiting":h.health?.status==="unreachable"?"Source unreachable":"Reachable / not flagged"]
  ];}
  function questionList(h,lang="english"){if(!h)return[];const it=lang==="italian",q=[];if(h.billsStatus==="unknown")q.push(it?"Le spese condominiali, elettricità, gas e internet sono inclusi nel canone?":"Are condominium charges, electricity, gas, and internet included in the rent?");if(h.deposit===null)q.push(it?"A quanto ammonta il deposito cauzionale?":"How much is the security deposit?");if(h.agencyFee===null)q.push(it?"Ci sono commissioni di agenzia o altri costi amministrativi?":"Are there any agency commissions or administrative fees?");if(h.contract==="unknown")q.push(it?"Che tipo di contratto di locazione viene offerto e qual è la durata minima?":"What type of rental contract is offered and what is the minimum stay?");if(!h.available)q.push(it?"Da quale data è disponibile l'alloggio?":"From what date is the property available?");if(!h.address)q.push(it?"Potrebbe confermare la posizione esatta dell'immobile?":"Could you confirm the exact location of the property?");if(h.furnished==="unknown")q.push(it?"L'alloggio è completamente arredato?":"Is the property fully furnished?");q.push(it?"È possibile fare una visita di persona o una videochiamata dal vivo prima di qualsiasi pagamento?":"Can I view the property in person or via a live video call before making any payment?");return q;}

  function addHistory(h,type,label,note=""){h.history=h.history||[];h.history.unshift({id:uid(),type,label,note,date:Date.now()});h.updatedAt=Date.now();}
  function setStatus(h,status,note=""){if(h.status===status)return;h.status=status;addHistory(h,"status",`Status → ${statusLabel(status)}`,note);}
  function saveHomes(){localStorage.setItem(STORAGE_KEY,JSON.stringify(homes));compareIds=compareIds.filter(id=>homes.some(h=>h.id===id)).slice(0,4);localStorage.setItem(COMPARE_KEY,JSON.stringify(compareIds));renderAll();}
  function migrateHomes(){if(localStorage.getItem(STORAGE_KEY))return;for(const key of LEGACY_KEYS){try{const data=JSON.parse(localStorage.getItem(key)||"[]");if(Array.isArray(data)&&data.length){homes=data.map(h=>normalizeHome(h));localStorage.setItem(STORAGE_KEY,JSON.stringify(homes));break;}}catch(_){}}}
  function loadHomes(){migrateHomes();try{homes=JSON.parse(localStorage.getItem(STORAGE_KEY)||"[]").map(normalizeHome);}catch(_){homes=[];}try{compareIds=JSON.parse(localStorage.getItem(COMPARE_KEY)||"[]");}catch(_){compareIds=[];}compareIds=Array.isArray(compareIds)?compareIds.filter(id=>homes.some(h=>h.id===id)).slice(0,4):[];}

  function showEditor(h,mode="import"){currentImported=h;currentConfidence=h.confidence||buildConfidence(h,mode==="import");$("editId").value=h.id||"";$("editUrl").value=h.url||"";$("editImage").value=h.image||"";$("editTitle").value=h.title||"";$("editSource").value=h.source||sourceFromUrl(h.url).label;$("editRent").value=h.rent??"";$("editBills").value=h.bills??"";$("editBillsStatus").value=h.billsStatus||"unknown";$("editDeposit").value=h.deposit??"";$("editAgencyFee").value=h.agencyFee??"";$("editType").value=h.type||"other";$("editArea").value=h.areaId||"unknown";$("editAddress").value=h.address||"";$("editSize").value=h.size??"";$("editRooms").value=h.rooms??"";$("editBedrooms").value=h.bedrooms??"";$("editBathrooms").value=h.bathrooms??"";$("editFloor").value=h.floor||"";$("editElevator").value=h.elevator||"unknown";$("editBalcony").value=h.balcony||"unknown";$("editAirConditioning").value=h.airConditioning||"unknown";$("editMinimumStay").value=h.minimumStay||"";$("editLandlordName").value=h.landlordName||"";$("editAvailable").value=h.available||"";$("editFurnished").value=h.furnished||"unknown";$("editContract").value=h.contract||"unknown";$("editStatus").value=h.status||"saved";$("editDescription").value=h.description||"";$("editNotes").value=h.notes||"";$("editorModeLabel").textContent=mode==="edit"?"EDIT SAVED HOME":mode==="manual"?"MANUAL HOME":"REVIEW IMPORT";$("listingEditorTitle").textContent=mode==="manual"?"Add the details you know.":mode==="edit"?"Update your saved home.":"Check what we found.";renderEditorPreview();qsa("[data-confidence]").forEach(el=>{const key=el.dataset.confidence==="area"?"areaId":el.dataset.confidence,level=currentConfidence[key]||"low";el.className=level;el.textContent=level==="high"?"found":level==="medium"?"review":"missing";});renderEditorCost();$("listingEditorDialog").showModal();if(!window.matchMedia("(max-width:760px)").matches)setTimeout(()=>$("editTitle").focus(),120);}
  function renderEditorPreview(){const h=currentImported||blankHome();$("editorPreview").innerHTML=`<div class="import-preview-media">${h.image?`<img src="${escapeAttr(h.image)}" alt="" referrerpolicy="no-referrer" onerror="this.remove()">`:`<span class="material-symbols-rounded">home</span>`}</div><div><span class="card-label">${escapeHtml(h.source||"Manual")}</span><h3 title="${escapeAttr(h.title||"Untitled rental")}">${escapeHtml(shortHomeTitle(h))}</h3><p>${h.url?escapeHtml(h.url):"No source URL"}</p></div>`;}
  function editorDraft(){const previous=homes.find(x=>x.id===$("editId").value)||currentImported||blankHome($("editUrl").value);return normalizeHome({...previous,id:$("editId").value||previous.id||uid(),url:$("editUrl").value||previous.url||"",image:$("editImage").value||previous.image||"",source:$("editSource").value||previous.source||"Manual",title:normalizeText($("editTitle").value)||"Untitled rental",rent:$("editRent").value===""?null:safeNumber($("editRent").value),bills:$("editBills").value===""?null:safeNumber($("editBills").value),billsStatus:$("editBillsStatus").value,deposit:$("editDeposit").value===""?null:safeNumber($("editDeposit").value),agencyFee:$("editAgencyFee").value===""?null:safeNumber($("editAgencyFee").value),type:$("editType").value,areaId:$("editArea").value,address:normalizeText($("editAddress").value),size:$("editSize").value===""?null:safeNumber($("editSize").value),rooms:$("editRooms").value===""?null:safeNumber($("editRooms").value),bedrooms:$("editBedrooms").value===""?null:safeNumber($("editBedrooms").value),bathrooms:$("editBathrooms").value===""?null:safeNumber($("editBathrooms").value),floor:normalizeText($("editFloor").value),elevator:$("editElevator").value,balcony:$("editBalcony").value,airConditioning:$("editAirConditioning").value,minimumStay:normalizeText($("editMinimumStay").value),landlordName:normalizeText($("editLandlordName").value),available:normalizeText($("editAvailable").value),furnished:$("editFurnished").value,contract:$("editContract").value,status:$("editStatus").value,description:normalizeText($("editDescription").value),notes:$("editNotes").value.trim(),confidence:currentConfidence,updatedAt:Date.now()});}
  function renderEditorCost(){if(!$("editorCostPreview"))return;const h=editorDraft();$("editorCostPreview").innerHTML=`<div class="cost-preview-cell"><span>REAL MONTHLY EST.</span><strong>${euro(monthlyEstimate(h))}</strong></div><div class="cost-preview-cell"><span>UPFRONT EST.</span><strong>${euro(upfrontEstimate(h))}</strong></div><div class="cost-preview-cell"><span>FIRST-YEAR EST.</span><strong>${euro(firstYearEstimate(h))}</strong></div>`;}
  function stringTokens(s){return new Set(normalizeText(s).toLowerCase().replace(/[^a-z0-9à-ÿ ]/g," ").split(/\s+/).filter(x=>x.length>2));}
  function jaccard(a,b){const A=stringTokens(a),B=stringTokens(b);if(!A.size||!B.size)return 0;const inter=[...A].filter(x=>B.has(x)).length;return inter/(A.size+B.size-inter);}
  function duplicateScore(a,b){if(a.url&&b.url&&normalizeUrl(a.url)===normalizeUrl(b.url))return 1;let s=0;if(a.address&&b.address)s+=jaccard(a.address,b.address)*.5;if(a.title&&b.title)s+=jaccard(a.title,b.title)*.2;if(a.areaId!=="unknown"&&a.areaId===b.areaId)s+=.12;if(a.rent!==null&&b.rent!==null&&Math.abs(a.rent-b.rent)<=Math.max(30,a.rent*.05))s+=.1;if(a.size&&b.size&&Math.abs(a.size-b.size)<=5)s+=.08;return s;}
  function findDuplicate(candidate){return homes.filter(h=>h.id!==candidate.id).map(h=>({home:h,score:duplicateScore(candidate,h)})).sort((a,b)=>b.score-a.score)[0];}
  function saveEditorHome(candidate,force=false){const existingIndex=homes.findIndex(h=>h.id===candidate.id);if(existingIndex<0&&!force){const d=findDuplicate(candidate);if(d&&d.score>=.62){duplicateState={candidate,existing:d.home,score:d.score};renderDuplicate();$("listingEditorDialog").close();$("duplicateDialog").showModal();return;}}if(existingIndex>=0){const old=homes[existingIndex];if(old.status!==candidate.status)addHistory(candidate,"status",`Status → ${statusLabel(candidate.status)}`);homes[existingIndex]=candidate;}else{addHistory(candidate,"saved","Saved to Home Hunt",candidate.source);homes.unshift(candidate);}$("listingEditorDialog").close();currentImported=null;saveHomes();toast(existingIndex>=0?"Home updated":"Home saved to My Homes");if(existingIndex<0&&window.matchMedia("(max-width:760px)").matches)setTimeout(()=>setMobileScreen("homes"),180);}
  function renderDuplicate(){const d=duplicateState;if(!d)return;const card=h=>`<div class="duplicate-home"><span>${escapeHtml(h.source)} · ${escapeHtml(areaById(h.areaId).name)}</span><h3>${escapeHtml(h.title)}</h3><p>${euro(h.rent)} rent · ${euro(monthlyEstimate(h))} monthly est. · ${fitScore(h)}/100 fit</p></div>`;$("duplicateContent").innerHTML=`<div class="duplicate-compare">${card(d.existing)}${card(d.candidate)}</div><p class="micro-note"><span class="material-symbols-rounded">join_inner</span>Match confidence: ${Math.round(d.score*100)}%. Merging keeps your existing status, favorite, notes, timeline, and fills missing property details from the new link.</p>`;}
  function mergeDuplicate(){const d=duplicateState;if(!d)return;const e=d.existing,c=d.candidate;const fields=["url","source","sourceId","title","rent","bills","billsStatus","deposit","agencyFee","type","areaId","address","size","rooms","bedrooms","bathrooms","floor","elevator","balcony","airConditioning","minimumStay","landlordName","available","furnished","contract","description","image","images","confidence"];fields.forEach(k=>{const ev=e[k],cv=c[k];const empty=!hasValue(ev)||ev==="unknown"||ev==="Rental listing"||ev==="Area not set"||(Array.isArray(ev)&&!ev.length);if(empty&&hasValue(cv))e[k]=cv;});e.updatedAt=Date.now();e.lastChecked=c.lastChecked||e.lastChecked;addHistory(e,"merge","Merged a possible duplicate",c.url);$("duplicateDialog").close();duplicateState=null;saveHomes();openWorkspace(e.id,"overview");toast("Listings merged");}

  async function importListing(url){clearImportAlert();hideImportSteps();const normalized=normalizeUrl(url);if(!normalized){showImportAlert("Enter a valid public rental listing URL, or add it manually.");return;}const source=sourceFromUrl(normalized);if(source.id==="other"){showImportAlert("This source is not available for automatic import. Keep the URL, then paste the listing text or add the details manually.");return;}if(!ACTIVE_IMPORTERS.has(source.id)){showImportAlert(`${escapeHtml(source.label)} automatic import is temporarily paused. <button type="button" id="alertTextFallback">Paste listing text</button> or <button type="button" id="alertManualFallback">add it manually</button>.`);setTimeout(()=>{$("alertTextFallback")?.addEventListener("click",()=>{$("listingTextInput")?.closest("details")?.setAttribute("open","");$("listingTextInput")?.focus();});$("alertManualFallback")?.addEventListener("click",()=>showEditor({...blankHome(normalized),source:source.label,sourceId:source.id},"manual"));},0);return;}const button=$("importListingBtn");button.disabled=true;const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),Number(IMPORT_CONFIG.requestTimeoutMs)||28000);try{setImportStep("detect");progress(true,10,`${source.label} detected`);await new Promise(r=>setTimeout(r,120));setImportStep("fetch");progress(true,30,IMPORT_CONFIG.importerApi?"Securely reading the listing…":"Reading the listing through fallback mode…");const parsed=await retrieveListing(normalized,controller.signal);setImportStep("extract");progress(true,67,"Extracting price, costs, property details, and images…");await new Promise(r=>setTimeout(r,100));setImportStep("analyze");progress(true,84,"Checking missing information and confidence…");parsed.confidence=parsed.confidence||buildConfidence(parsed,true);await new Promise(r=>setTimeout(r,100));setImportStep("review","done");progress(true,100,"Ready for your review");setTimeout(()=>{progress(false);hideImportSteps();showEditor(parsed,"import");const mode=parsed.importMethod==="api"?"secure importer":parsed.importMethod==="api-reader-fallback"?"secure importer fallback":"reader fallback";showImportAlert(`Imported from <b>${escapeHtml(parsed.source)}</b> via ${mode}. Review every field before saving.`,"success");},180);}catch(err){progress(false);hideImportSteps();const msg=err?.name==="AbortError"?"The listing took too long to read.":escapeHtml(err?.message||"The listing could not be read automatically.");showImportAlert(`${msg} <button type="button" id="alertTextFallback">paste listing text</button> or <button type="button" id="alertManualFallback">add the details manually</button>.`);setTimeout(()=>{$("alertTextFallback")?.addEventListener("click",()=>{$("listingTextInput")?.focus();$("listingTextInput")?.closest("details")?.setAttribute("open","");});$("alertManualFallback")?.addEventListener("click",()=>showEditor({...blankHome(normalized),source:source.label,sourceId:source.id},"manual"));},0);}finally{clearTimeout(timer);button.disabled=false;}}
  function importPastedText(){clearImportAlert();const text=$("listingTextInput")?.value.trim()||"",url=normalizeUrl($("listingUrl").value);if(text.length<80){showImportAlert("Paste a larger section of the listing text so Home Hunt has enough information to extract.");return;}const parsed=parseListing(text,url);parsed.importMethod="pasted-text";parsed.source=url?sourceFromUrl(url).label:"Pasted listing";parsed.sourceId=url?sourceFromUrl(url).id:"other";parsed.confidence=buildConfidence(parsed,true);showEditor(parsed,"import");showImportAlert("Extracted the text you pasted. Review the fields before saving.","success");}

  function healthLabel(h){if(h.health?.status==="changed")return["Changed","changed"];if(h.health?.status==="unreachable")return["Source issue","unreachable"];if(h.lastChecked)return["Checked",""];return["Not checked",""];}
  function homeSearchText(h){return[h.title,h.source,areaById(h.areaId).name,h.address,h.notes,statusLabel(h.status)].join(" ").toLowerCase();}
  function filteredHomes(){const query=$("homesSearch").value.trim().toLowerCase(),status=$("homesStatusFilter").value,sort=$("homesSort").value;let r=homes.filter(h=>(!query||homeSearchText(h).includes(query))&&(status==="all"||h.status===status)&&(!favoriteOnly||h.favorite));r.sort((a,b)=>{if(sort==="value")return valueScore(b)-valueScore(a);if(sort==="firstYear")return(firstYearEstimate(a)??Infinity)-(firstYearEstimate(b)??Infinity);if(sort==="monthly")return(monthlyEstimate(a)??Infinity)-(monthlyEstimate(b)??Infinity);if(sort==="commute")return(commuteEstimate(a)??Infinity)-(commuteEstimate(b)??Infinity);if(sort==="recent")return(b.createdAt||0)-(a.createdAt||0);return fitScore(b)-fitScore(a);});return r;}
  function renderKanban(){const board=$("kanbanBoard");board.innerHTML=PIPELINE.map(status=>{const list=homes.filter(h=>h.status===status);return`<section class="kanban-column" data-drop-status="${status}"><div class="kanban-column-head"><strong>${escapeHtml(STATUS[status].short)}</strong><span>${list.length}</span></div><div class="kanban-stack">${list.map(h=>`<article class="kanban-card ${h.favorite?"favorite":""}" draggable="true" data-drag-home="${h.id}" data-open-home="${h.id}"><b>${escapeHtml(h.title)}</b><small>${escapeHtml(areaById(h.areaId).name)} · ${euro(monthlyEstimate(h))}/mo</small><div class="kanban-card-bottom"><span>${commuteLabel(h)}</span><strong>${fitScore(h)}/100</strong></div></article>`).join("")||`<div class="contact-empty"><small>Drop a home here</small></div>`}</div></section>`;}).join("");qsa("[data-drag-home]",board).forEach(card=>{card.addEventListener("dragstart",e=>{e.dataTransfer.setData("text/home-id",card.dataset.dragHome);e.dataTransfer.effectAllowed="move";});card.addEventListener("click",e=>{if(!e.defaultPrevented)openWorkspace(card.dataset.dragHome);});});qsa("[data-drop-status]",board).forEach(col=>{col.addEventListener("dragover",e=>{e.preventDefault();col.classList.add("drag-over");});col.addEventListener("dragleave",()=>col.classList.remove("drag-over"));col.addEventListener("drop",e=>{e.preventDefault();col.classList.remove("drag-over");const id=e.dataTransfer.getData("text/home-id"),h=homes.find(x=>x.id===id);if(h){setStatus(h,col.dataset.dropStatus,"Moved on the pipeline board");saveHomes();}});});}
  function renderHomes(){const list=filteredHomes();$("homesEmpty").hidden=homes.length>0;$("homesGrid").innerHTML=list.map(h=>{const warnings=warningList(h),compare=compareIds.includes(h.id),short=shortHomeTitle(h);return`<article class="home-card" data-home-card="${h.id}" role="button" tabindex="0" aria-label="Open brief for ${escapeAttr(short)}"><div class="home-card-media">${h.image?`<img src="${escapeAttr(h.image)}" alt="" loading="lazy" referrerpolicy="no-referrer" onerror="this.style.display='none'">`:`<div class="home-card-placeholder"><span class="material-symbols-rounded">apartment</span></div>`}<div class="home-card-badges"><span class="status-badge">${escapeHtml(STATUS[h.status]?.short||"Saved")}</span></div><button class="favorite-btn ${h.favorite?"active":""}" data-favorite="${h.id}" aria-label="Favorite"><span class="material-symbols-rounded">favorite</span></button></div><div class="home-card-body"><div class="home-source-row"><span>${escapeHtml(h.source)}</span><small>${warnings.length?`${warnings.length} to verify`:"ready to review"}</small></div><h3 title="${escapeAttr(h.title)}">${escapeHtml(short)}</h3><div class="home-location"><span class="material-symbols-rounded">location_on</span>${escapeHtml(displayLocation(h))}</div><div class="home-price-row"><strong>${euro(h.rent)} <small>rent</small></strong><span>est. monthly<b>${euro(monthlyEstimate(h))}</b></span></div><div class="home-quick-stats"><span><b>${fitScore(h)}</b> fit</span><span><b>${commuteLabel(h)}</b> commute</span>${warnings.length?`<span class="needs-check"><b>${warnings.length}</b> checks</span>`:`<span class="is-ready"><b>✓</b> complete</span>`}</div><div class="home-card-actions">${h.url?`<a href="${escapeAttr(h.url)}" target="_blank" rel="noopener noreferrer" data-original-link><span class="material-symbols-rounded">open_in_new</span>Listing</a>`:`<button type="button" disabled><span class="material-symbols-rounded">link_off</span>No link</button>`}<button class="compare-action ${compare?"active":""}" data-compare="${h.id}" type="button"><span class="material-symbols-rounded">compare_arrows</span>${compare?"Added":"Match"}</button></div></div></article>`;}).join("");qsa("[data-home-card]").forEach(card=>{const open=e=>{if(e.target.closest("button,a,input,select,textarea,label"))return;openQuickView(card.dataset.homeCard);};card.addEventListener("click",open);card.addEventListener("keydown",e=>{if((e.key==="Enter"||e.key===" ")&&!e.target.closest("button,a")){e.preventDefault();openQuickView(card.dataset.homeCard);}});});qsa("[data-favorite]").forEach(b=>b.addEventListener("click",e=>{e.stopPropagation();const h=homes.find(x=>x.id===b.dataset.favorite);h.favorite=!h.favorite;addHistory(h,"favorite",h.favorite?"Marked as favorite":"Removed from favorites");saveHomes();}));qsa("[data-compare]").forEach(b=>b.addEventListener("click",e=>{e.stopPropagation();toggleCompare(b.dataset.compare);}));}

  function dueActions(){const today=new Date(`${todayIso()}T23:59:59`);return homes.filter(h=>h.nextAction?.date&&new Date(`${h.nextAction.date}T12:00:00`)<=today&&!['chosen','rejected','unavailable'].includes(h.status));}
  function upcomingViewings(){const now=new Date(),end=new Date(Date.now()+7*86400000);return homes.filter(h=>h.status==="viewing"&&h.nextAction?.date).filter(h=>{const d=new Date(`${h.nextAction.date}T12:00:00`);return d>=now&&d<=end;});}
  function renderDashboard(){const due=dueActions(),viewings=upcomingViewings(),changed=homes.filter(h=>h.health?.status==="changed"),favorites=homes.filter(h=>h.favorite),best=homes.slice().sort((a,b)=>fitScore(b)-fitScore(a))[0],value=homes.slice().filter(h=>monthlyEstimate(h)!==null).sort((a,b)=>valueScore(b)-valueScore(a))[0];const cards=[
    ["schedule",due.length,"Follow-ups due",due.length ? `${due[0].title.slice(0,45)} needs attention` : "No overdue next actions","myHomes"],
    ["event",viewings.length,"Viewings this week",viewings.length ? `${viewings[0].title.slice(0,45)} is coming up` : "No viewing scheduled","myHomes"],
    ["difference",changed.length,"Listing changes",changed.length?"Review source changes before accepting them":"No pending source changes","myHomes"],
    ["favorite",favorites.length,"Favorites",favorites.length?`Top favorite: ${favorites[0].title.slice(0,38)}`:"Star homes you want to protect from the noise","myHomes"],
    ["stars",best?fitScore(best):0,"Best fit",best?`${best.title.slice(0,45)} · ${euro(monthlyEstimate(best))}/mo`:"Save homes to calculate your best fit","compareHomes"],
    ["history",homes.length?1:0,"Recently saved",homes.length?`${homes.slice().sort((a,b)=>(b.createdAt||0)-(a.createdAt||0))[0].title.slice(0,45)}`:"Your newest serious candidate appears here","myHomes"]
  ];$("dashboardGrid").innerHTML=cards.map(([icon,n,title,p,target])=>`<article class="dash-card" data-scroll-target="${target}"><div class="dash-card-top"><span class="material-symbols-rounded">${icon}</span><span class="card-label">LIVE SHORTLIST</span></div><div class="dash-number">${n}</div><strong>${title}</strong><p>${escapeHtml(p)}</p><small>Open pipeline →</small></article>`).join("");qsa("[data-scroll-target]").forEach(c=>c.addEventListener("click",()=>document.getElementById(c.dataset.scrollTarget)?.scrollIntoView({behavior:"smooth"})));$("heroCommandGrid").innerHTML=`<div class="hero-command-stat"><span>SAVED</span><strong>${homes.length}</strong><small>${favorites.length} favorites</small></div><div class="hero-command-stat"><span>CONTACTED+</span><strong>${homes.filter(h=>['contacted','replied','viewing','finalist','chosen'].includes(h.status)).length}</strong><small>${homes.filter(h=>h.status==='replied').length} replies</small></div><div class="hero-command-stat"><span>BEST FIT</span><strong>${best?fitScore(best):0}</strong><small>${best?escapeHtml(best.title.slice(0,25)):"No homes yet"}</small></div><div class="hero-command-stat"><span>BEST VALUE</span><strong>${value?euro(monthlyEstimate(value)):"—"}</strong><small>${value?escapeHtml(areaById(value.areaId).name):"Save a listing"}</small></div>`;$("heroSavedCount").textContent=`${homes.length} home${homes.length===1?"":"s"}`;const next=$("heroNextAction");if(due.length){next.href="#myHomes";next.innerHTML=`<span class="material-symbols-rounded">priority_high</span><span><b>${due.length} follow-up${due.length===1?"":"s"} due</b><small>${escapeHtml(due[0].title.slice(0,40))}</small></span><span class="material-symbols-rounded">arrow_forward</span>`;}else if(viewings.length){next.href="#myHomes";next.innerHTML=`<span class="material-symbols-rounded">event</span><span><b>Viewing coming up</b><small>${escapeHtml(viewings[0].title.slice(0,40))}</small></span><span class="material-symbols-rounded">arrow_forward</span>`;}else{next.href=homes.length?"#myHomes":"#quickAdd";next.innerHTML=`<span class="material-symbols-rounded">task_alt</span><span><b>${homes.length?"Shortlist is organized":"Start your shortlist"}</b><small>${homes.length?"Open your strongest homes":"Save your first real listing"}</small></span><span class="material-symbols-rounded">arrow_forward</span>`;}
    const p=getProfile();const steps=[{done:true,n:"01",t:"Choose campus",d:CAMPUS[p.campus].name},{done:homes.length>0,n:"02",t:"Save first home",d:homes.length?`${homes.length} saved`:"Paste a listing URL"},{done:homes.some(h=>['contacted','replied','viewing','finalist','chosen'].includes(h.status)),n:"03",t:"Contact landlords",d:"Track outreach"},{done:homes.some(h=>['finalist','chosen'].includes(h.status)),n:"04",t:"Compare finalists",d:"Choose with evidence"}];$("onboardingSteps").innerHTML=steps.map(s=>`<div class="onboard-step ${s.done?"done":""}"><b>${s.done?"✓":s.n}</b><strong>${s.t}</strong><span>${escapeHtml(s.d)}</span></div>`).join("");$("onboardingPanel").hidden=homes.some(h=>h.status==="chosen");}

  function toggleCompare(id){if(compareIds.includes(id))compareIds=compareIds.filter(x=>x!==id);else if(compareIds.length<4)compareIds.push(id);else{toast("Compare up to four homes");return;}localStorage.setItem(COMPARE_KEY,JSON.stringify(compareIds));renderCompare();renderHomes();}
  function minima(list,fn){const vals=list.map(h=>({h,v:fn(h)})).filter(x=>x.v!==null&&Number.isFinite(x.v));return vals.sort((a,b)=>a.v-b.v)[0]?.h||null;}
  function maxima(list,fn){return list.slice().sort((a,b)=>fn(b)-fn(a))[0]||null;}
  function renderCompare(){const list=compareIds.map(id=>homes.find(h=>h.id===id)).filter(Boolean),enough=list.length>=2;$("compareEmpty").hidden=enough;$("compareShell").hidden=!enough;if(!enough){$("comparisonMobile").innerHTML="";$("compareEmpty").innerHTML=list.length===1?`<span class="material-symbols-rounded">looks_two</span><div><strong>One home selected.</strong><p>Add one more home to see a useful comparison.</p><a class="compare-empty-link" href="#myHomes">Choose another saved home</a></div>`:`<span class="material-symbols-rounded">compare_arrows</span><div><strong>No match yet.</strong><p>Add 2–4 saved homes to compare cost, commute and fit.</p><a class="compare-empty-link" href="#myHomes">Open saved homes</a></div>`;return;}const winners={monthly:minima(list,monthlyEstimate),upfront:minima(list,upfrontEstimate),commute:minima(list,commuteEstimate),complete:maxima(list,importCompleteness),fit:maxima(list,fitScore),value:maxima(list,valueScore)};const insightData=[["payments","Lowest monthly",winners.monthly,`${euro(monthlyEstimate(winners.monthly||{}))}/mo`],["savings","Lowest upfront",winners.upfront,euro(upfrontEstimate(winners.upfront||{}))],["directions_transit","Fastest trip",winners.commute,winners.commute?commuteLabel(winners.commute):"—"],["stars","Best fit",winners.fit,winners.fit?`${fitScore(winners.fit)}/100`:"—"]];const sortedFit=list.slice().sort((a,b)=>fitScore(b)-fitScore(a));let tradeoff="";if(sortedFit.length>=2){const a=sortedFit[0],b=sortedFit[1],cost=(monthlyEstimate(a)??0)-(monthlyEstimate(b)??0),time=(commuteEstimate(b)??0)-(commuteEstimate(a)??0);tradeoff=cost>0&&time>0?`${shortHomeTitle(a)} costs ${euro(cost)} more each month but saves about ${time} minutes per campus trip.`:`${shortHomeTitle(a)} currently leads your fit score.`;}$("comparisonInsights").innerHTML=insightData.map(([i,l,h,v])=>`<div class="insight-chip"><span><span class="material-symbols-rounded" style="font-size:15px;vertical-align:middle">${i}</span> ${l}</span><strong>${h?escapeHtml(shortHomeTitle(h)):"—"}</strong><p>${v}</p></div>`).join("")+(tradeoff?`<div class="insight-chip tradeoff-insight"><span>QUICK TAKE</span><strong>What matters most</strong><p>${escapeHtml(tradeoff)}</p></div>`:"");$("comparisonMobile").innerHTML=list.map((h,i)=>{const badges=[];if(winners.monthly?.id===h.id)badges.push("Lowest monthly");if(winners.upfront?.id===h.id)badges.push("Low upfront");if(winners.fit?.id===h.id)badges.push("Best fit");return`<article class="compare-mobile-card"><div class="compare-mobile-head">${h.image?`<img src="${escapeAttr(h.image)}" alt="" referrerpolicy="no-referrer">`:`<span class="compare-mobile-placeholder material-symbols-rounded">apartment</span>`}<div><span>HOME ${String.fromCharCode(65+i)}</span><h3>${escapeHtml(shortHomeTitle(h))}</h3><small>${escapeHtml(areaById(h.areaId).name)}</small></div></div>${badges.length?`<div class="compare-mobile-badges">${badges.map(x=>`<span>${x}</span>`).join("")}</div>`:""}<div class="compare-mobile-metrics"><div><span>Rent</span><b>${euro(h.rent)}</b></div><div><span>Monthly</span><b>${euro(monthlyEstimate(h))}</b></div><div><span>Upfront</span><b>${euro(upfrontEstimate(h))}</b></div><div><span>Commute</span><b>${commuteLabel(h)}</b></div><div><span>Fit</span><b>${fitScore(h)}/100</b></div><div><span>Checks</span><b>${warningList(h).length}</b></div></div><button type="button" class="compare-open-brief" data-compare-open="${h.id}"><span class="material-symbols-rounded">visibility</span>Quick view</button></article>`;}).join("");const rows=[["Rent",h=>euro(h.rent),"rent"],["Monthly",h=>euro(monthlyEstimate(h)),"monthly"],["Upfront",h=>euro(upfrontEstimate(h)),"upfront"],["1st year",h=>euro(firstYearEstimate(h)),""],["Commute",h=>commuteLabel(h),"commute"],["Deposit",h=>euro(h.deposit),""],["Admin fee",h=>euro(h.agencyFee),""],["Furnished",h=>h.furnished,""],["Available",h=>h.available||"Confirm",""],["Completeness",h=>`${importCompleteness(h)}%`,"complete"],["Checks",h=>warningList(h).length,""],["Fit",h=>`${fitScore(h)}/100`,"fit"]];$("compareTable").innerHTML=`<thead><tr><th>Factor</th>${list.map((h,i)=>`<th><span class="compare-home-letter">${String.fromCharCode(65+i)}</span><strong>${escapeHtml(shortHomeTitle(h))}</strong><small>${escapeHtml(areaById(h.areaId).name)}</small></th>`).join("")}</tr></thead><tbody>${rows.map(([label,fn,wkey])=>`<tr><th>${label}</th>${list.map(h=>`<td>${escapeHtml(fn(h))}${wkey&&winners[wkey]?.id===h.id?`<span class="compare-winner">BEST</span>`:""}</td>`).join("")}</tr>`).join("")}</tbody>`;qsa("[data-compare-open]").forEach(b=>b.addEventListener("click",()=>openQuickView(b.dataset.compareOpen)));}
  function shareSummaryText(){const list=(compareIds.length?compareIds.map(id=>homes.find(h=>h.id===id)).filter(Boolean):homes.filter(h=>h.status==="finalist").slice(0,4));if(!list.length)return"No finalist homes selected yet.";return`Milan Home Hunt shortlist\n${list.map((h,i)=>`\n${i+1}. ${h.title}\n${areaById(h.areaId).name} · ${euro(monthlyEstimate(h))}/month · ${commuteLabel(h)} to ${CAMPUS[getProfile().campus].name} · fit ${fitScore(h)}/100\nFirst-year est.: ${euro(firstYearEstimate(h))}\n${h.url||"No source link"}`).join("\n")}\n\nGenerated from my private Home Hunt shortlist. Verify all listing details with the original source.`;}
  function openShareDialog(){const text=shareSummaryText();$("sharePreview").textContent=text;$("shareDialog").showModal();}

  function quickViewHtml(h){const warnings=warningList(h),short=shortHomeTitle(h);return`<div class="quick-view-media">${h.image?`<img src="${escapeAttr(h.image)}" alt="" referrerpolicy="no-referrer">`:`<div class="quick-view-placeholder"><span class="material-symbols-rounded">apartment</span></div>`}</div><div class="quick-view-body"><div class="quick-view-source"><span>${escapeHtml(h.source)}</span><span>${escapeHtml(displayLocation(h))}</span></div><h2>${escapeHtml(short)}</h2>${h.title!==short?`<p class="quick-view-full-title">${escapeHtml(h.title)}</p>`:""}<div class="quick-view-price"><strong>${euro(h.rent)}</strong><span>advertised rent</span></div><div class="quick-view-grid"><div><span>Est. monthly</span><b>${euro(monthlyEstimate(h))}</b></div><div><span>Upfront</span><b>${euro(upfrontEstimate(h))}</b></div><div><span>Commute</span><b>${commuteLabel(h)}</b></div><div><span>Fit</span><b>${fitScore(h)}/100</b></div></div><div class="quick-view-check ${warnings.length?"needs":"ready"}"><span class="material-symbols-rounded">${warnings.length?"fact_check":"verified"}</span><div><b>${warnings.length?`${warnings.length} detail${warnings.length===1?"":"s"} to verify`:"Key listing details look complete"}</b><small>${warnings.length?escapeHtml(warnings.slice(0,2).join(" · ")):"Still confirm everything with the original source before paying."}</small></div></div><div class="quick-view-actions">${h.url?`<a class="hunt-primary-btn" href="${escapeAttr(h.url)}" target="_blank" rel="noopener noreferrer"><span class="material-symbols-rounded">open_in_new</span>Open listing</a>`:""}<button class="hunt-secondary-btn" type="button" data-quick-details="${h.id}"><span class="material-symbols-rounded">receipt_long</span>Full details</button><button class="hunt-ghost-btn ${compareIds.includes(h.id)?"active":""}" type="button" data-quick-compare="${h.id}"><span class="material-symbols-rounded">compare_arrows</span>${compareIds.includes(h.id)?"In match":"Add to match"}</button></div></div>`;}
  function bindQuickViewActions(h){$("quickViewContent").querySelector("[data-quick-details]")?.addEventListener("click",()=>{$("quickViewDialog").close();openWorkspace(h.id);});$("quickViewContent").querySelector("[data-quick-compare]")?.addEventListener("click",()=>{toggleCompare(h.id);$("quickViewContent").innerHTML=quickViewHtml(h);bindQuickViewActions(h);});}
  function openQuickView(id){const h=homes.find(x=>x.id===id);if(!h)return;$("quickViewStatus").textContent=STATUS[h.status]?.short||"Saved";$("quickViewContent").innerHTML=quickViewHtml(h);$("quickViewDialog").showModal();bindQuickViewActions(h);}

  function mapDirectionsUrl(h,mode="transit"){const origin=h.address?`${h.address}, Milano, Italy`:areaById(h.areaId).name+", Milano, Italy";return`https://www.google.com/maps/dir/?api=1&origin=${encodeURIComponent(origin)}&destination=${encodeURIComponent(CAMPUS[getProfile().campus].address)}&travelmode=${encodeURIComponent(mode)}`;}
  function workspaceMediaHtml(h){const imgs=(h.images&&h.images.length?h.images:[h.image]).filter(Boolean).slice(0,6);if(!imgs.length)return`<div class="workspace-placeholder"><span class="material-symbols-rounded">apartment</span></div>`;return`<img id="workspaceMainImage" src="${escapeAttr(imgs[0])}" alt="" referrerpolicy="no-referrer"><div class="workspace-thumbs">${imgs.map((img,i)=>`<button type="button" class="${i===0?"active":""}" data-gallery-image="${escapeAttr(img)}"><img src="${escapeAttr(img)}" alt="" referrerpolicy="no-referrer"></button>`).join("")}</div>`;}
  function renderWorkspace(id,tab=workspaceTab){const h=homes.find(x=>x.id===id);if(!h)return;currentWorkspaceId=id;workspaceTab=tab;$("workspaceTitle").textContent="Property details";$("workspaceSubtitle").textContent=`${h.source} · ${STATUS[h.status]?.short||"Saved"} · ${fmtDate(h.createdAt)}`;const tabs=[["overview","Details"],["commute","Travel"],["verify","Check"],["timeline","Activity"],["source","Source"]];let panel="";if(tab==="overview")panel=workspaceOverview(h);if(tab==="commute")panel=workspaceCommute(h);if(tab==="verify")panel=workspaceVerify(h);if(tab==="timeline")panel=workspaceTimeline(h);if(tab==="source")panel=workspaceSource(h);$("workspaceContent").innerHTML=`<div class="workspace-hero"><div class="workspace-media">${workspaceMediaHtml(h)}</div><div class="workspace-summary"><div class="workspace-summary-top"><span class="workspace-source">${escapeHtml(h.source)} · ${escapeHtml(statusLabel(h.status))}</span><button class="favorite-btn ${h.favorite?"active":""}" style="position:static" data-ws-favorite="${h.id}"><span class="material-symbols-rounded">favorite</span></button></div><h2 title="${escapeAttr(h.title)}">${escapeHtml(shortHomeTitle(h))}</h2><div class="workspace-location"><span class="material-symbols-rounded">location_on</span>${escapeHtml(displayLocation(h))}</div><div class="workspace-price">${euro(h.rent)} <small>advertised rent</small></div><div class="workspace-kpis"><div class="workspace-kpi"><span>REAL MONTHLY</span><strong>${euro(monthlyEstimate(h))}</strong></div><div class="workspace-kpi"><span>UPFRONT</span><strong>${euro(upfrontEstimate(h))}</strong></div><div class="workspace-kpi"><span>1ST YEAR</span><strong>${euro(firstYearEstimate(h))}</strong></div><div class="workspace-kpi"><span>FIT</span><strong>${fitScore(h)}/100</strong></div></div></div></div><nav class="workspace-tabs">${tabs.map(([k,l])=>`<button class="${tab===k?"active":""}" data-ws-tab="${k}" type="button">${l}</button>`).join("")}</nav><div class="workspace-panel">${panel}</div>`;bindWorkspaceActions(h);}
  function workspaceOverview(h){const facts=[["Type",TYPE_LABEL[h.type]||h.type],["Area",areaById(h.areaId).name],["Size",h.size?`${h.size} m²`:"—"],["Rooms",h.rooms??"—"],["Bedrooms",h.bedrooms??"—"],["Bathrooms",h.bathrooms??"—"],["Floor",h.floor||"—"],["Furnished",h.furnished],["Elevator",h.elevator],["Balcony",h.balcony],["A/C",h.airConditioning],["Minimum stay",h.minimumStay||"—"],["Available",h.available||"—"],["Contract",h.contract],["Landlord / agency",h.landlordName||"—"]];return`<div class="workspace-grid"><section class="workspace-section"><span class="card-label">PROPERTY DETAILS</span><h3>Saved snapshot</h3><div class="detail-list">${facts.map(([a,b])=>`<div class="detail-row"><span>${a}</span><b>${escapeHtml(b)}</b></div>`).join("")}</div></section><section class="workspace-section"><span class="card-label">COST STACK</span><h3>What this home really costs</h3><div class="detail-list">${[["Advertised rent",euro(h.rent)],["Bills / charges",h.billsStatus==="included"?"Included":h.bills!==null?euro(h.bills):`~${euro(estimatedBills(h))} planning estimate`],["Deposit",euro(h.deposit)],["Agency/admin",euro(h.agencyFee)],["Monthly estimate",euro(monthlyEstimate(h))],["First-year estimate",euro(firstYearEstimate(h))]].map(([a,b])=>`<div class="detail-row"><span>${a}</span><b>${b}</b></div>`).join("")}</div></section><section class="workspace-section span-2"><span class="card-label">PRIVATE NOTES</span><h3>Your decision notes</h3><textarea id="wsNotes" rows="4" placeholder="Impressions, deal-breakers, landlord details…">${escapeHtml(h.notes||"")}</textarea><div class="workspace-status-row"><label><span>Pipeline status</span><select id="wsStatus">${Object.entries(STATUS).map(([key,value])=>`<option value="${key}" ${h.status===key?"selected":""}>${escapeHtml(value.label)}</option>`).join("")}</select></label><button class="hunt-secondary-btn" data-ws-save-status type="button">Update status</button></div><div class="route-actions"><button class="hunt-secondary-btn" data-ws-save-notes type="button">Save notes</button><button class="hunt-secondary-btn" data-ws-edit type="button"><span class="material-symbols-rounded">edit</span>Edit property fields</button>${h.url?`<a class="hunt-secondary-btn" href="${escapeAttr(h.url)}" target="_blank" rel="noopener noreferrer"><span class="material-symbols-rounded">open_in_new</span>Original listing</a>`:""}</div></section></div>`;}
  function workspaceCommute(h){const v=h.verifiedCommute||{};return`<div class="workspace-grid"><section class="workspace-section span-2"><span class="card-label">VERIFIED COMMUTE</span><h3>Check the real route, then save the time you see.</h3><p class="micro-note">Home Hunt does not invent live public-transit times. Open the exact route in your maps app, then store the confirmed transit/walk/bike time. Verified transit replaces neighborhood estimates in scoring.</p><div class="route-controls"><label class="route-mode"><span>TRANSIT MINUTES</span><strong>${v.transit?`${v.transit} min verified`:"Not verified"}</strong><input id="wsTransit" type="number" min="1" max="180" value="${v.transit??""}" placeholder="e.g. 24"></label><label class="route-mode"><span>WALK MINUTES</span><strong>${v.walk?`${v.walk} min`:"Optional"}</strong><input id="wsWalk" type="number" min="1" max="300" value="${v.walk??""}"></label><label class="route-mode"><span>BIKE MINUTES</span><strong>${v.bike?`${v.bike} min`:"Optional"}</strong><input id="wsBike" type="number" min="1" max="180" value="${v.bike??""}"></label></div><div class="route-actions"><a class="hunt-primary-btn" href="${mapDirectionsUrl(h,"transit")}" target="_blank" rel="noopener noreferrer"><span class="material-symbols-rounded">directions_transit</span>Check route to ${escapeHtml(CAMPUS[getProfile().campus].name)}</a><button class="hunt-secondary-btn" data-ws-save-commute type="button">Save verified times</button></div></section><section class="workspace-section"><span class="card-label">SCORING IMPACT</span><h3>${commuteLabel(h)} to campus</h3><div class="detail-list"><div class="detail-row"><span>Target</span><b>≤ ${getProfile().commute} min</b></div><div class="detail-row"><span>Current score component</span><b>${Math.round(componentScores(h).commute)}/100</b></div><div class="detail-row"><span>Campus</span><b>${escapeHtml(CAMPUS[getProfile().campus].full)}</b></div></div></section><section class="workspace-section"><span class="card-label">ROUTE SOURCE</span><h3>${h.address?"Exact listing location available":"Area-level origin"}</h3><p class="micro-note">${h.address?escapeHtml(h.address):`No exact address was saved. The route opens from ${escapeHtml(areaById(h.areaId).name)} instead.`}</p></section></div>`;}
  function workspaceVerify(h){const items=verificationItems(h);return`<div class="workspace-grid"><section class="workspace-section span-2"><span class="card-label">EVIDENCE CHECK</span><h3>Verified / missing / needs clarification</h3><div class="verification-grid">${items.map(([label,ok,detail])=>`<div class="verification-item ${ok?"ok":"missing"}"><b>${ok?"✓":"?"} ${escapeHtml(label)}</b><span>${escapeHtml(detail)}</span></div>`).join("")}</div></section><section class="workspace-section"><span class="card-label">QUESTIONS</span><h3>Ask what is missing</h3><div class="question-list">${questionList(h,"english").map(q=>`<div class="question-item"><span class="material-symbols-rounded">help</span><span>${escapeHtml(q)}</span></div>`).join("")}</div></section><section class="workspace-section"><span class="card-label">RISK FRAMING</span><h3>${warningList(h).length} verification gap${warningList(h).length===1?"":"s"}</h3><p class="micro-note">This is not a “scam score.” A complete-looking listing can still be fraudulent, and an incomplete listing can be legitimate. Verify identity, property, contract, and payment instructions independently.</p></section></div>`;}
  function workspaceTimeline(h){return`<div class="workspace-grid"><section class="workspace-section"><span class="card-label">NEXT ACTION</span><h3>Keep momentum</h3><div class="next-action-grid"><label><span>Action type</span><select id="wsNextType"><option value="" ${!h.nextAction.type?"selected":""}>Choose</option><option value="followup" ${h.nextAction.type==="followup"?"selected":""}>Follow up</option><option value="viewing" ${h.nextAction.type==="viewing"?"selected":""}>Viewing</option><option value="documents" ${h.nextAction.type==="documents"?"selected":""}>Send documents</option><option value="decision" ${h.nextAction.type==="decision"?"selected":""}>Decision deadline</option></select></label><label><span>Date</span><input id="wsNextDate" type="date" value="${escapeAttr(h.nextAction.date||"")}"></label></div><label style="margin-top:8px"><span>Note</span><input id="wsNextNote" type="text" value="${escapeAttr(h.nextAction.note||"")}" placeholder="What should happen next?"></label><div class="route-actions"><button class="hunt-primary-btn" data-ws-save-next type="button">Save next action</button>${h.nextAction.date?`<button class="hunt-secondary-btn" data-ws-calendar type="button"><span class="material-symbols-rounded">event</span>Add to calendar</button>`:""}</div></section><section class="workspace-section"><span class="card-label">ADD TIMELINE NOTE</span><h3>Record a contact or decision</h3><div class="timeline-add"><input id="wsTimelineNote" type="text" placeholder="e.g. Landlord replied; bills are €80"><button class="hunt-secondary-btn" data-ws-add-history type="button">Add</button></div></section><section class="workspace-section span-2"><span class="card-label">CONTACT HISTORY</span><h3>${h.history.length} event${h.history.length===1?"":"s"}</h3><div class="timeline">${h.history.length?h.history.map(e=>`<div class="timeline-item"><i class="timeline-dot"></i><div><strong>${escapeHtml(e.label)}</strong>${e.note?`<p>${escapeHtml(e.note)}</p>`:""}</div><time>${fmtDateTime(e.date)}</time></div>`).join(""):`<div class="contact-empty"><p>No activity recorded yet.</p></div>`}</div></section></div>`;}
  function workspaceSource(h){const health=healthLabel(h);return`<div class="workspace-grid"><section class="workspace-section span-2"><span class="card-label">LISTING HEALTH</span><h3>${health[0]}</h3><div class="health-summary"><div class="health-box"><span>LAST CHECK</span><strong>${h.lastChecked?fmtDateTime(h.lastChecked):"Never"}</strong></div><div class="health-box"><span>SOURCE STATUS</span><strong>${health[0]}</strong></div><div class="health-box"><span>SAVED SNAPSHOT</span><strong>${fmtDateTime(h.updatedAt||h.createdAt)}</strong></div></div><div class="route-actions"><button class="hunt-primary-btn" data-ws-refresh type="button"><span class="material-symbols-rounded">refresh</span>Check source now</button>${h.url?`<a class="hunt-secondary-btn" href="${escapeAttr(h.url)}" target="_blank" rel="noopener noreferrer">Open source</a>`:""}</div><p class="micro-note">Refresh compares the public source with your saved record. Changes are shown as a diff and are never applied without your approval.</p></section><section class="workspace-section"><span class="card-label">SOURCE LINK</span><h3>${escapeHtml(h.source)}</h3><p class="micro-note" style="word-break:break-all">${escapeHtml(h.url||"No source URL saved")}</p></section><section class="workspace-section"><span class="card-label">HOUSEKEEPING</span><h3>Manage this record</h3><div class="route-actions"><button class="hunt-secondary-btn" data-ws-edit type="button">Edit fields</button><button class="hunt-secondary-btn" data-ws-delete type="button">Delete home</button></div></section></div>`;}
  function bindWorkspaceActions(h){qsa("[data-ws-tab]").forEach(b=>b.addEventListener("click",()=>renderWorkspace(h.id,b.dataset.wsTab)));qsa("[data-gallery-image]").forEach(b=>b.addEventListener("click",()=>{const main=$("workspaceMainImage");if(main)main.src=b.dataset.galleryImage;qsa("[data-gallery-image]").forEach(x=>x.classList.toggle("active",x===b));}));qs("[data-ws-favorite]")?.addEventListener("click",()=>{h.favorite=!h.favorite;addHistory(h,"favorite",h.favorite?"Marked as favorite":"Removed from favorites");saveHomes();renderWorkspace(h.id,workspaceTab);});qsa("[data-ws-edit]").forEach(b=>b.addEventListener("click",()=>{currentConfidence=h.confidence||buildConfidence(h,false);$("workspaceDialog").close();showEditor(h,"edit");}));qs("[data-ws-save-status]")?.addEventListener("click",()=>{const next=$("wsStatus")?.value;if(next){setStatus(h,next,"Updated from property workspace");saveHomes();renderWorkspace(h.id,"overview");toast("Status updated");}});qs("[data-ws-save-notes]")?.addEventListener("click",()=>{h.notes=$("wsNotes").value.trim();addHistory(h,"note","Updated private notes");saveHomes();toast("Notes saved");});qs("[data-ws-save-commute]")?.addEventListener("click",()=>{h.verifiedCommute={transit:safeNumber($("wsTransit").value),walk:safeNumber($("wsWalk").value),bike:safeNumber($("wsBike").value),checkedAt:Date.now()};addHistory(h,"commute","Verified commute updated",h.verifiedCommute.transit?`${h.verifiedCommute.transit} min transit`:"");saveHomes();renderWorkspace(h.id,"commute");toast("Commute saved");});qs("[data-ws-save-next]")?.addEventListener("click",()=>{h.nextAction={type:$("wsNextType").value,date:$("wsNextDate").value,note:normalizeText($("wsNextNote").value)};if(h.nextAction.type==="viewing"&&h.status!=="viewing")setStatus(h,"viewing","Viewing scheduled");addHistory(h,"next","Next action updated",[h.nextAction.type,h.nextAction.date,h.nextAction.note].filter(Boolean).join(" · "));saveHomes();renderWorkspace(h.id,"timeline");toast("Next action saved");});qs("[data-ws-add-history]")?.addEventListener("click",()=>{const note=normalizeText($("wsTimelineNote").value);if(!note)return;addHistory(h,"note","Timeline note",note);saveHomes();renderWorkspace(h.id,"timeline");});qs("[data-ws-calendar]")?.addEventListener("click",()=>downloadIcs(h));qs("[data-ws-refresh]")?.addEventListener("click",()=>refreshHome(h));qs("[data-ws-delete]")?.addEventListener("click",()=>{if(confirm("Delete this home from your local shortlist?")){homes=homes.filter(x=>x.id!==h.id);$("workspaceDialog").close();saveHomes();}});}
  function openWorkspace(id,tab="overview"){$("workspaceDialog").showModal();renderWorkspace(id,tab);}
  function downloadIcs(h){if(!h.nextAction?.date)return;const d=h.nextAction.date.replaceAll("-","");const summary=`Home Hunt: ${h.nextAction.type||"Housing action"} — ${h.title}`.replace(/[,;]/g," ");const desc=(h.nextAction.note||h.url||"").replace(/\n/g,"\\n").replace(/,/g,"\\,");const text=`BEGIN:VCALENDAR\nVERSION:2.0\nPRODID:-//Polimi Students//Home Hunt//EN\nBEGIN:VEVENT\nUID:${h.id}@polimi-homehunt\nDTSTART;VALUE=DATE:${d}\nSUMMARY:${summary}\nDESCRIPTION:${desc}\nEND:VEVENT\nEND:VCALENDAR`;downloadText(`home-hunt-${d}.ics`,text,"text/calendar");}

  const DIFF_FIELDS=["title","rent","bills","billsStatus","deposit","agencyFee","type","areaId","address","size","rooms","bedrooms","bathrooms","floor","elevator","balcony","airConditioning","minimumStay","landlordName","available","furnished","contract","description","image"];
  function changedFields(oldH,newH){return DIFF_FIELDS.filter(k=>JSON.stringify(oldH[k]??null)!==JSON.stringify(newH[k]??null));}
  async function refreshHome(h){if(!h.url){toast("This home has no source URL");return;}toast("Checking source…");const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),Number(IMPORT_CONFIG.requestTimeoutMs)||28000);try{const fresh=await retrieveListing(h.url,controller.signal),changes=changedFields(h,fresh);h.lastChecked=Date.now();if(!changes.length){h.health={status:"ok",changedAt:null,message:"No public changes detected"};addHistory(h,"refresh","Source checked","No changes detected");saveHomes();renderWorkspace(h.id,"source");toast("No source changes detected");return;}h.health={status:"changed",changedAt:Date.now(),message:`${changes.length} change(s) waiting for review`};diffState={home:h,fresh,changes};renderDiff();saveHomes();$("diffDialog").showModal();}catch(err){h.lastChecked=Date.now();h.health={status:"unreachable",changedAt:Date.now(),message:"The public page could not be read"};addHistory(h,"refresh","Source check failed",err?.message||"Public listing page could not be read automatically");saveHomes();renderWorkspace(h.id,"source");toast("Source could not be read");}finally{clearTimeout(timer);}}
  function formatDiffValue(k,v){if(["rent","bills","deposit","agencyFee"].includes(k))return euro(v);if(k==="areaId")return areaById(v).name;if(v===null||v==="")return"—";return String(v).slice(0,180);}
  function renderDiff(){const d=diffState;if(!d)return;$("diffList").innerHTML=d.changes.map(k=>`<label class="diff-item"><input type="checkbox" data-diff-field="${k}" checked><strong>${escapeHtml(k.replace(/([A-Z])/g," $1"))}</strong><div class="diff-old"><b>SAVED</b><br>${escapeHtml(formatDiffValue(k,d.home[k]))}</div><div class="diff-new"><b>SOURCE NOW</b><br>${escapeHtml(formatDiffValue(k,d.fresh[k]))}</div></label>`).join("");}
  function applyDiff(){const d=diffState;if(!d)return;const selected=qsa("[data-diff-field]:checked").map(x=>x.dataset.diffField);selected.forEach(k=>d.home[k]=d.fresh[k]);d.home.lastChecked=Date.now();d.home.health={status:"ok",changedAt:null,message:selected.length?"Approved source changes applied":"Changes reviewed without applying"};addHistory(d.home,"refresh","Reviewed source changes",selected.length?`Applied: ${selected.join(", ")}`:"No changes applied");$("diffDialog").close();diffState=null;saveHomes();if(currentWorkspaceId===d?.home?.id)renderWorkspace(currentWorkspaceId,"source");toast("Source review saved");}

  function renderContactDesk(){const sel=$("contactHomeSelect"),current=sel.value;sel.innerHTML=`<option value="">Choose a saved home</option>${homes.map(h=>`<option value="${h.id}">${escapeHtml(h.title)} · ${escapeHtml(areaById(h.areaId).name)}</option>`).join("")}`;if(homes.some(h=>h.id===current))sel.value=current;renderQuestions();}
  function renderQuestions(){const h=homes.find(x=>x.id===$("contactHomeSelect").value);const list=$("questionList");if(!h){list.innerHTML=`<div class="contact-empty"><span class="material-symbols-rounded">quiz</span><p>Choose a home to generate only the questions its listing leaves unanswered.</p></div>`;return;}list.innerHTML=questionList(h,messageLanguage).map(q=>`<div class="question-item"><span class="material-symbols-rounded">help</span><span>${escapeHtml(q)}</span></div>`).join("");}
  function contactProfile(){try{return JSON.parse(localStorage.getItem(CONTACT_KEY)||"{}");}catch(_){return{};}}
  function saveContactProfile(){localStorage.setItem(CONTACT_KEY,JSON.stringify({name:$("contactName").value,program:$("contactProgram").value,moveIn:$("contactMoveIn").value,duration:$("contactDuration").value}));}
  function generateMessage(){const h=homes.find(x=>x.id===$("contactHomeSelect").value);if(!h){toast("Choose a saved home first");return;}saveContactProfile();const it=messageLanguage==="italian",name=normalizeText($("contactName").value)||"a prospective tenant",program=normalizeText($("contactProgram").value)||"a master's student at Politecnico di Milano",move=normalizeText($("contactMoveIn").value)||"the advertised date",duration=normalizeText($("contactDuration").value)||"a long-term stay",type=$("messageType").value,qs=questionList(h,messageLanguage).slice(0,4);let body="";if(!it){const intro=`Hello, my name is ${name}. I am ${program}. I am interested in the listing “${h.title}” in ${areaById(h.areaId).name}.`;const details=`I am looking to move in around ${move} for ${duration}.`;const templates={first:`${intro}\n\n${details}\n\nCould you please confirm whether the property is still available?${qs.length?` I would also like to clarify:\n- ${qs.join("\n- ")}`:""}\n\nThank you. I would be happy to arrange a viewing or video call.`,followup:`Hello, this is ${name}. I am following up regarding “${h.title}”. I remain interested and wanted to ask whether there is any update on availability or next steps.\n\nThank you.`,viewing:`${intro}\n\nWould it be possible to arrange a viewing, or a live video viewing if I am not yet in Milan? Please let me know what dates/times work for you.\n\nThank you.`,costs:`${intro}\n\nBefore moving forward, could you please clarify the full monthly and upfront costs?\n- ${qs.join("\n- ")}\n\nThank you.`,availability:`${intro}\n\nCould you please confirm that the property is still available from ${move}, and whether ${duration} is acceptable?\n\nThank you.`,decline:`Hello, thank you for your time regarding “${h.title}”. I have decided not to proceed with this property, but I appreciate your help and wish you all the best.`};body=templates[type]||templates.first;}else{const intro=`Buongiorno, mi chiamo ${name}. Sono ${program}. Sono interessato/a all'annuncio “${h.title}” in zona ${areaById(h.areaId).name}.`;const details=`Sto cercando una sistemazione da circa ${move} per ${duration}.`;const templates={first:`${intro}\n\n${details}\n\nPotrebbe confermarmi se l'alloggio è ancora disponibile?${qs.length?` Vorrei inoltre chiarire:\n- ${qs.join("\n- ")}`:""}\n\nGrazie. Sarei felice di organizzare una visita o una videochiamata.`,followup:`Buongiorno, sono ${name}. La ricontatto in merito a “${h.title}”. Sono ancora interessato/a e volevo sapere se ci sono aggiornamenti sulla disponibilità o sui prossimi passi.\n\nGrazie.`,viewing:`${intro}\n\nSarebbe possibile organizzare una visita, oppure una video-visita dal vivo se non sono ancora a Milano? Mi faccia sapere quali giorni e orari sono disponibili.\n\nGrazie.`,costs:`${intro}\n\nPrima di procedere, potrebbe chiarirmi tutti i costi mensili e iniziali?\n- ${qs.join("\n- ")}\n\nGrazie.`,availability:`${intro}\n\nPotrebbe confermare che l'alloggio è ancora disponibile da ${move} e che una permanenza di ${duration} è possibile?\n\nGrazie.`,decline:`Buongiorno, grazie per il tempo dedicato all'annuncio “${h.title}”. Ho deciso di non procedere con questo alloggio, ma la ringrazio per la disponibilità.`};body=templates[type]||templates.first;}$("landlordMessage").value=body;$("copyLandlordMessage").disabled=false;if(type!=="decline"&&h.status==="saved")setStatus(h,"contacted","Message prepared in Contact Desk");saveHomes();}

  function renderDocs(){let state={};try{state=JSON.parse(localStorage.getItem(DOCS_KEY)||"{}");}catch(_){}$("documentChecklist").innerHTML=DOCS.map(([id,title,note])=>`<label class="doc-check"><input type="checkbox" data-doc-id="${id}" ${state[id]?"checked":""}><span><strong>${title}</strong><small>${note}</small></span></label>`).join("");const update=()=>{const s={};qsa("[data-doc-id]").forEach(x=>s[x.dataset.docId]=x.checked);localStorage.setItem(DOCS_KEY,JSON.stringify(s));const n=Object.values(s).filter(Boolean).length,p=Math.round(n/DOCS.length*100);$("readinessPercent").textContent=`${p}%`;$("readinessBar").style.width=`${p}%`;};qsa("[data-doc-id]").forEach(x=>x.addEventListener("change",update));update();}
  function renderWeights(){const w=getWeights();$("weightControls").innerHTML=Object.entries(WEIGHT_LABELS).map(([k,l])=>`<label class="weight-row"><span>${l}</span><input type="range" min="0" max="100" step="5" value="${w[k]}" data-weight="${k}"><b data-weight-value="${k}">${w[k]}</b></label>`).join("");const update=()=>{const x={};qsa("[data-weight]").forEach(i=>{x[i.dataset.weight]=Number(i.value);qs(`[data-weight-value="${i.dataset.weight}"]`).textContent=i.value;});localStorage.setItem(WEIGHTS_KEY,JSON.stringify(x));const total=Object.values(x).reduce((a,b)=>a+b,0);$("weightTotal").innerHTML=`Raw weight total: <b>${total}</b>. Scores are normalized, so the total does not need to equal 100.`;renderAll();};qsa("[data-weight]").forEach(i=>i.addEventListener("input",update));update();}

  function renderAll(){document.body.classList.toggle("hh-has-homes",homes.length>0);renderDashboard();renderKanban();renderHomes();renderCompare();renderContactDesk();if(currentWorkspaceId&&$("workspaceDialog").open&&homes.some(h=>h.id===currentWorkspaceId))renderWorkspace(currentWorkspaceId,workspaceTab);}

  function exportHomes(){const payload={version:4,exportedAt:new Date().toISOString(),profile:getProfile(),weights:getWeights(),homes};downloadText(`polimi-home-hunt-backup-${todayIso()}.json`,JSON.stringify(payload,null,2),"application/json");}
  async function restoreHomes(file){try{const data=JSON.parse(await file.text()),list=Array.isArray(data)?data:data.homes;if(!Array.isArray(list))throw new Error();homes=list.map(normalizeHome);if(data.profile)localStorage.setItem(PROFILE_KEY,JSON.stringify(data.profile));if(data.weights)localStorage.setItem(WEIGHTS_KEY,JSON.stringify(data.weights));saveHomes();toast(`${homes.length} homes restored`);location.reload();}catch(_){toast("That backup file could not be restored");}}
  function bookmarkletText(){const base=new URL("housing.html",location.href).href;return`javascript:(()=>{location.href='${base}?url='+encodeURIComponent(location.href)})()`;}
  function parseSharedUrl(){const params=new URLSearchParams(location.search);let candidate=params.get("url")||"";if(!candidate){const text=params.get("text")||"";candidate=text.match(/https?:\/\/\S+/)?.[0]||"";}candidate=normalizeUrl(candidate);if(candidate){$("listingUrl").value=candidate;updateSourceDetection();history.replaceState({},"",location.pathname);setMobileScreen("add",{scroll:false});toast("Shared listing ready to import");}}
  function initInstall(){window.addEventListener("beforeinstallprompt",e=>{e.preventDefault();deferredInstallPrompt=e;$("installPwaBtn").hidden=false;});$("installPwaBtn").addEventListener("click",async()=>{if(!deferredInstallPrompt)return;deferredInstallPrompt.prompt();await deferredInstallPrompt.userChoice;deferredInstallPrompt=null;$("installPwaBtn").hidden=true;});}
  function initServiceWorker(){if("serviceWorker" in navigator&&location.protocol.startsWith("http")){navigator.serviceWorker.register("./sw.js").catch(()=>{});}}

  function bindEvents(){
    $("copySearchBrief").addEventListener("click",()=>copyText(searchBriefText(),"Search brief copied"));
    $("listingUrl").addEventListener("input",updateSourceDetection);$("listingUrl").addEventListener("keydown",e=>{if(e.key==="Enter"){e.preventDefault();importListing($("listingUrl").value);}});
    $("pasteListingUrl").addEventListener("click",async()=>{try{$("listingUrl").value=await navigator.clipboard.readText();updateSourceDetection();}catch(_){toast("Clipboard access was blocked; paste manually");}});
    $("importListingBtn").addEventListener("click",()=>importListing($("listingUrl").value));$("importTextBtn")?.addEventListener("click",importPastedText);$("manualAddBtn").addEventListener("click",()=>showEditor(blankHome(normalizeUrl($("listingUrl").value)),"manual"));
    ["copyBookmarkletBtn","copyBookmarkletBtn2"].forEach(id=>$(id)?.addEventListener("click",()=>copyText(bookmarkletText(),"Bookmarklet copied — create a bookmark and paste this into its URL field")));
    $("shareInstallHelpBtn").addEventListener("click",()=>toast("Install the PWA where supported, then use your browser’s Share menu → Home Hunt. Copy/paste remains the universal fallback."));
    $("listingEditorForm").addEventListener("submit",e=>{e.preventDefault();saveEditorHome(editorDraft());});$("cancelEditorBtn").addEventListener("click",()=>$("listingEditorDialog").close());
    qsa("#listingEditorForm input,#listingEditorForm select").forEach(el=>el.addEventListener("input",renderEditorCost));
    $("closeWorkspaceBtn").addEventListener("click",()=>$("workspaceDialog").close());
    $("closeQuickViewBtn").addEventListener("click",()=>$("quickViewDialog").close());
    $("openScoreSettings").addEventListener("click",()=>{renderWeights();$("scoreDialog").showModal();});qsa("[data-open-score]").forEach(b=>b.addEventListener("click",()=>{renderWeights();$("scoreDialog").showModal();}));qsa("[data-open-tool]").forEach(b=>b.addEventListener("click",()=>{const d=$(b.dataset.openTool);if(!d)return;d.open=true;setTimeout(()=>d.scrollIntoView({behavior:"smooth",block:"start"}),50);}));$("resetWeightsBtn").addEventListener("click",()=>{localStorage.setItem(WEIGHTS_KEY,JSON.stringify(DEFAULT_WEIGHTS));renderWeights();renderAll();});
    $("homesSearch").addEventListener("input",renderHomes);$("homesStatusFilter").addEventListener("change",renderHomes);$("homesSort").addEventListener("change",renderHomes);$("favoritesOnly").addEventListener("click",()=>{favoriteOnly=!favoriteOnly;$("favoritesOnly").classList.toggle("active",favoriteOnly);$("favoritesOnly").setAttribute("aria-pressed",String(favoriteOnly));renderHomes();});
    $("clearCompareBtn").addEventListener("click",()=>{compareIds=[];localStorage.setItem(COMPARE_KEY,"[]");renderAll();});$("shareShortlistBtn").addEventListener("click",openShareDialog);
    $("closeShareBtn").addEventListener("click",()=>$("shareDialog").close());$("copyShareSummaryBtn").addEventListener("click",()=>copyText(shareSummaryText(),"Shortlist summary copied"));$("nativeShareBtn").addEventListener("click",async()=>{const text=shareSummaryText();if(navigator.share)try{await navigator.share({title:"Milan Home Hunt shortlist",text});}catch(_){}else copyText(text,"Shortlist copied");});
    $("closeDuplicateBtn").addEventListener("click",()=>{$("duplicateDialog").close();duplicateState=null;});$("saveDuplicateAnywayBtn").addEventListener("click",()=>{const c=duplicateState?.candidate;$("duplicateDialog").close();duplicateState=null;if(c)saveEditorHome(c,true);});$("mergeDuplicateBtn").addEventListener("click",mergeDuplicate);
    $("closeDiffBtn").addEventListener("click",()=>$("diffDialog").close());$("dismissDiffBtn").addEventListener("click",()=>{if(diffState){diffState.home.health={status:"changed",changedAt:Date.now(),message:"Changes reviewed but not applied"};addHistory(diffState.home,"refresh","Source changes reviewed","Kept saved values");saveHomes();}$("diffDialog").close();diffState=null;});$("applyDiffBtn").addEventListener("click",applyDiff);
    $("exportHomesBtn").addEventListener("click",exportHomes);$("restoreHomesInput").addEventListener("change",e=>{if(e.target.files[0])restoreHomes(e.target.files[0]);e.target.value="";});
    $("contactHomeSelect").addEventListener("change",()=>{renderQuestions();$("landlordMessage").value="";$("copyLandlordMessage").disabled=true;});$("messageType").addEventListener("change",()=>$("landlordMessage").value="");
    qsa("[data-language]").forEach(b=>b.addEventListener("click",()=>{messageLanguage=b.dataset.language;qsa("[data-language]").forEach(x=>x.classList.toggle("active",x===b));renderQuestions();}));
    $("generateMessageBtn").addEventListener("click",generateMessage);$("copyLandlordMessage").addEventListener("click",()=>copyText($("landlordMessage").value,"Landlord message copied"));
    $("openSearchGuideBtn")?.addEventListener("click",()=>{const d=$("mainSearchGuideDisclosure");if(d){d.open=true;requestAnimationFrame(()=>d.scrollIntoView({behavior:"smooth",block:"start"}));}});
  }


  const MOBILE_SCREEN_KEY="polimi_home_hunt_mobile_screen_v1";
  const MOBILE_SCREEN_MAP={quickAdd:"home",dashboard:"home",myHomes:"homes",compareHomes:"compare",toolsHub:"tools",searchGuide:"home",contactDesk:"tools",toolkit:"tools",finalCheckTitle:"tools"};
  function setMobileScreen(requested="home",opts={}){
    const mq=window.matchMedia("(max-width:760px)");
    const screen=requested==="add"?"home":requested;
    if(!mq.matches){qsa("[data-mobile-screen]").forEach(el=>el.classList.add("mobile-screen-active"));return;}
    const valid=["home","homes","compare","tools"].includes(screen)?screen:"home";
    qsa("[data-mobile-screen]").forEach(el=>el.classList.toggle("mobile-screen-active",el.dataset.mobileScreen===valid));
    qsa("[data-hh-screen]").forEach(btn=>btn.classList.toggle("active",btn.dataset.hhScreen===valid));
    if(requested!=="add")localStorage.setItem(MOBILE_SCREEN_KEY,valid);
    if(opts.scroll!==false)window.scrollTo({top:0,behavior:opts.instant?"auto":"smooth"});
    if(requested==="add")setTimeout(()=>{const input=$("listingUrl");input?.focus({preventScroll:true});$("quickAdd")?.scrollIntoView({behavior:"smooth",block:"start"});},120);
  }
  function initMobileShell(){
    const mq=window.matchMedia("(max-width:760px)");
    const apply=()=>{
      if(mq.matches){const saved=localStorage.getItem(MOBILE_SCREEN_KEY)||"home";setMobileScreen(saved,{scroll:false,instant:true});}
      else qsa("[data-mobile-screen]").forEach(el=>el.classList.add("mobile-screen-active"));
    };
    qsa("[data-hh-screen]").forEach(btn=>btn.addEventListener("click",()=>setMobileScreen(btn.dataset.hhScreen)));
    document.addEventListener("click",e=>{
      if(!mq.matches)return;
      const a=e.target.closest('a[href^="#"]');if(!a)return;
      const id=(a.getAttribute("href")||"").slice(1),screen=MOBILE_SCREEN_MAP[id];if(!screen)return;
      e.preventDefault();setMobileScreen(screen);
      const target=$(id);const disclosure=target?.closest("details");if(disclosure)disclosure.open=true;
    });
    if(mq.addEventListener)mq.addEventListener("change",apply);else mq.addListener?.(apply);
    apply();
  }

  function initAreas(){const html=AREAS.map(a=>`<option value="${a.id}">${escapeHtml(a.name)}</option>`).join("");$("editArea").innerHTML=html;}
  function initContact(){const p=contactProfile();$("contactName").value=p.name||"";$("contactProgram").value=p.program||"";$("contactMoveIn").value=p.moveIn||"";$("contactDuration").value=p.duration||"";
  }
  function init(){initTheme();initAreas();initProfile();loadHomes();initContact();renderDocs();bindEvents();initMobileShell();initInstall();initServiceWorker();renderAll();parseSharedUrl();checkImporterStatus();}
  document.addEventListener("DOMContentLoaded",init);
})();
